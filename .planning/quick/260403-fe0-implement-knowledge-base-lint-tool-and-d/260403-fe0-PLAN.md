# Quick Task 260403-fe0 Plan

**Description:** Implement knowledge-base lint tool and docs/roadmap updates for agentic wiki loop.  
**Опис:** Реалізувати інструмент lint для knowledge base та оновити docs/roadmap для циклу agentic wiki.

## Scope

1. Add MCP tool `lint_knowledge_base(...)` for Markdown knowledge-base diagnostics.
2. Add tests for lint behavior and MCP tool surface updates.
3. Update project docs and planning artifacts for the new capability.

## Tasks

### Task 1: Lint module and server integration
- Add `src/turbo_memory_mcp/knowledge_lint.py` with deterministic checks:
  - broken internal links
  - orphan candidates
  - duplicate titles
- Expose tool via `server.py` as `lint_knowledge_base(paths=None, max_issues=200)`.
- Update contracts tool list and self-test payload expectations.

### Task 2: Test coverage
- Add unit tests for lint logic and payload shape.
- Update MCP tool catalog tests and smoke contract tests for the new tool.

### Task 3: Documentation and roadmap
- Update:
  - `README.md`, `README.uk.md`, `README.ru.md`
  - `TECHNICAL_SPEC.md`, `TECHNICAL_SPEC.uk.md`, `TECHNICAL_SPEC.ru.md`
  - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`
- Document the new knowledge-base maintenance loop and lint tool use.

## Verification

1. Run targeted tests for lint + tool contract.
2. Run full `pytest` if targeted tests pass.
3. Ensure new docs match implemented tool surface.

