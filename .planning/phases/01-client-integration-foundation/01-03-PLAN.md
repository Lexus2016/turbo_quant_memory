---
phase: 01-client-integration-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/smoke_test.py
  - tests/test_smoke_contract.py
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04]
user_setup: []
must_haves:
  truths:
    - "The repo can launch the packaged server as a child process and exercise the four Phase 1 tools over real MCP stdio."
    - "The server metadata used by docs and clients is regression-tested in code."
  artifacts:
    - "scripts/smoke_test.py"
    - "tests/test_smoke_contract.py"
  key_links:
    - "`scripts/smoke_test.py` launches the same `uv run turbo-memory-mcp serve` command documented in README and config fixtures."
    - "`tests/test_smoke_contract.py` locks the same server metadata that `server_info` and `self_test` expose."
---

<objective>
Add the real stdio smoke path and the contract regression layer.

Purpose: prove that Phase 1 is not just packaged and documented, but can also be validated locally through real MCP traffic.
Output: MCP stdio smoke script and contract regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-integration-foundation/01-CONTEXT.md
@.planning/phases/01-client-integration-foundation/01-RESEARCH.md
@.planning/phases/01-client-integration-foundation/01-VALIDATION.md
@CLIENT_INTEGRATIONS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the end-to-end stdio smoke script</name>
  <files>scripts/smoke_test.py</files>
  <read_first>.planning/phases/01-client-integration-foundation/01-RESEARCH.md, pyproject.toml, src/turbo_memory_mcp/cli.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/contracts.py</read_first>
  <action>Create `scripts/smoke_test.py` using the official MCP Python client over stdio. Launch the server with `StdioServerParameters(command="uv", args=["run", "turbo-memory-mcp", "serve"])`, initialize a `ClientSession`, list tools, and assert that the names are exactly `health`, `server_info`, `list_scopes`, and `self_test`. Call all four tools and validate: `health.status == "ok"`, `server_info.runtime_command == "turbo-memory-mcp serve"`, `list_scopes` contains `project`, `global`, and `hybrid`, and `self_test.tool_count == 4`. Print a compact pass/fail summary and exit non-zero on any mismatch.</action>
  <verify>uv run python scripts/smoke_test.py</verify>
  <acceptance_criteria>
    - `scripts/smoke_test.py` contains `StdioServerParameters`
    - `scripts/smoke_test.py` contains `uv`, `run`, `turbo-memory-mcp`, and `serve`
    - `scripts/smoke_test.py` calls `health`, `server_info`, `list_scopes`, and `self_test`
    - `uv run python scripts/smoke_test.py` exits 0
  </acceptance_criteria>
  <done>The repo has a real MCP-level smoke path that exercises the packaged server over stdio.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract regression tests for the smoke metadata</name>
  <files>tests/test_smoke_contract.py</files>
  <read_first>scripts/smoke_test.py, src/turbo_memory_mcp/contracts.py, tests/test_tools.py</read_first>
  <action>Add `tests/test_smoke_contract.py` to lock the metadata that later docs and external clients rely on. Assert that `server_info` returns `runtime_command == "turbo-memory-mcp serve"`, `server_id == "tqmemory"`, Tier 1 clients exactly `["Claude Code", "Codex", "Cursor", "OpenCode"]`, Tier 2 clients exactly `["Antigravity"]`, and `self_test.tool_names` exactly match the exported Phase 1 tool list. Keep the tests deterministic and offline.</action>
  <verify>uv run pytest -q tests/test_smoke_contract.py</verify>
  <acceptance_criteria>
    - `tests/test_smoke_contract.py` contains `turbo-memory-mcp serve`
    - `tests/test_smoke_contract.py` contains `tqmemory`
    - `tests/test_smoke_contract.py` contains `Claude Code`, `Codex`, `Cursor`, `OpenCode`, and `Antigravity`
    - `uv run pytest -q tests/test_smoke_contract.py` exits 0
  </acceptance_criteria>
  <done>The repo has regression coverage for the exact metadata that the client docs and smoke checklist depend on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python scripts/smoke_test.py` succeeds
- [ ] `uv run pytest -q tests/test_smoke_contract.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- The Phase 1 server can be exercised over real MCP stdio from inside the repo
- The contract metadata used by docs and clients is regression-tested
- The automated validation path stays independent from GUI/editor-specific client hosts
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-integration-foundation/01-03-SUMMARY.md`
</output>
