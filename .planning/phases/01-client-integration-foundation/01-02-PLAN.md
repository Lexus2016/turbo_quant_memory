---
phase: 01-client-integration-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - README.md
  - examples/clients/claude.project.mcp.json
  - examples/clients/codex.config.toml
  - examples/clients/cursor.project.mcp.json
  - examples/clients/opencode.config.json
  - examples/clients/antigravity.mcp.json
  - examples/clients/SMOKE_CHECKLIST.md
autonomous: true
requirements: [INT-03, INT-04]
user_setup: []
must_haves:
  truths:
    - "A new operator can see the install path, runtime command, and client-tier support without reverse engineering planning docs."
    - "Each target client has a checked-in example config that launches the same `tqmemory` stdio server."
  artifacts:
    - "README.md"
    - "examples/clients/claude.project.mcp.json"
    - "examples/clients/codex.config.toml"
    - "examples/clients/cursor.project.mcp.json"
    - "examples/clients/opencode.config.json"
    - "examples/clients/antigravity.mcp.json"
    - "examples/clients/SMOKE_CHECKLIST.md"
  key_links:
    - "README.md repeats the same runtime command and server id that `server_info` returns."
    - "All example configs use the same server identifier `tqmemory` and the same `uv run turbo-memory-mcp serve` launch contract."
    - "The Antigravity example is clearly documented as Tier 2 compatibility, not equal proof to Tier 1."
    - "`examples/clients/SMOKE_CHECKLIST.md` references the checked-in config fixtures under `examples/clients/`."
---

<objective>
Publish the operator-facing quickstart and the canonical client config fixtures.

Purpose: make the Phase 1 server easy to install, connect, and understand from the repo itself.
Output: README quickstart plus example MCP configs for Claude Code, Codex, Cursor, OpenCode, and Antigravity.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-integration-foundation/01-CONTEXT.md
@.planning/phases/01-client-integration-foundation/01-RESEARCH.md
@CLIENT_INTEGRATIONS.md
@TECHNICAL_SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the README quickstart and client support contract</name>
  <files>README.md</files>
  <read_first>.planning/phases/01-client-integration-foundation/01-CONTEXT.md, .planning/phases/01-client-integration-foundation/01-RESEARCH.md, CLIENT_INTEGRATIONS.md, pyproject.toml, src/turbo_memory_mcp/contracts.py</read_first>
  <action>Create `README.md` as a bilingual EN/UK operator document. Add the sections `## Quickstart`, `## Runtime Contract`, `## Supported Clients`, `## Tier 1`, and `## Tier 2`. Document the canonical install path `uv sync`, the canonical runtime `uv run turbo-memory-mcp serve`, the `pip install -e .` fallback, the server id `tqmemory`, and the exact four Phase 1 tools. Add a compatibility table covering Claude Code, Codex, Cursor, OpenCode, and Antigravity with a tier column. Explicitly state that Phase 1 proves integration and introspection only, not the real memory loop, and do not claim direct KV-cache control over hosted models.</action>
  <verify>rg -n "## Quickstart|## Runtime Contract|## Supported Clients|tqmemory|turbo-memory-mcp serve|self_test" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Quickstart`
    - `README.md` contains `uv run turbo-memory-mcp serve`
    - `README.md` contains `tqmemory`
    - `README.md` contains `Claude Code`, `Codex`, `Cursor`, `OpenCode`, and `Antigravity`
  </acceptance_criteria>
  <done>The repository has a self-contained onboarding document that matches the actual Phase 1 runtime and support contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical config fixtures for every target client</name>
  <files>examples/clients/claude.project.mcp.json, examples/clients/codex.config.toml, examples/clients/cursor.project.mcp.json, examples/clients/opencode.config.json, examples/clients/antigravity.mcp.json</files>
  <read_first>README.md, CLIENT_INTEGRATIONS.md, pyproject.toml, src/turbo_memory_mcp/contracts.py</read_first>
  <action>Create `examples/clients/` and add five canonical example configs. `examples/clients/claude.project.mcp.json` must use the `mcpServers` JSON shape with `command: "uv"` and `args: ["run", "turbo-memory-mcp", "serve"]`. `examples/clients/codex.config.toml` must define `[mcp_servers.tqmemory]` with `command = "uv"` and matching `args`. `examples/clients/cursor.project.mcp.json` must use the Cursor `mcpServers` JSON shape. `examples/clients/opencode.config.json` must include `$schema`, `mcp.tqmemory.type = "local"`, and the command array `["uv","run","turbo-memory-mcp","serve"]`. `examples/clients/antigravity.mcp.json` must mirror the raw `mcpServers` JSON shape documented for manual import. Use the same server name `tqmemory` everywhere and leave `env`/`environment` empty by default.</action>
  <verify>python -m json.tool examples/clients/claude.project.mcp.json >/dev/null && python -m json.tool examples/clients/cursor.project.mcp.json >/dev/null && python -m json.tool examples/clients/opencode.config.json >/dev/null && python -m json.tool examples/clients/antigravity.mcp.json >/dev/null && python -c "import pathlib, tomllib; tomllib.loads(pathlib.Path('examples/clients/codex.config.toml').read_text())"</verify>
  <acceptance_criteria>
    - `examples/clients/claude.project.mcp.json` contains `"tqmemory"`
    - `examples/clients/codex.config.toml` contains `[mcp_servers.tqmemory]`
    - `examples/clients/cursor.project.mcp.json` contains `"command": "uv"`
    - `examples/clients/opencode.config.json` contains `"type": "local"`
    - `examples/clients/antigravity.mcp.json` contains `"mcpServers"`
  </acceptance_criteria>
  <done>Every target client has a checked-in starter config that points to the same stdio server contract.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the manual smoke checklist for all clients</name>
  <files>examples/clients/SMOKE_CHECKLIST.md</files>
  <read_first>README.md, examples/clients/claude.project.mcp.json, examples/clients/codex.config.toml, examples/clients/cursor.project.mcp.json, examples/clients/opencode.config.json, examples/clients/antigravity.mcp.json</read_first>
  <action>Create `examples/clients/SMOKE_CHECKLIST.md` as a bilingual EN/UK checklist. Add one subsection each for Claude Code, Codex, Cursor, OpenCode, and Antigravity. For the Tier 1 clients, include: how to load the checked-in example config or add the server, how to confirm the server is visible in the host, and what prompt or action should run `self_test`. For Antigravity, include the raw-config import path and clearly label it as `Tier 2` and `documented compatibility target in Phase 1`. Use precise commands and expected success signals, and avoid vague instructions like "verify it works".</action>
  <verify>rg -n "Claude Code|Codex|Cursor|OpenCode|Antigravity|self_test|Tier 2" examples/clients/SMOKE_CHECKLIST.md</verify>
  <acceptance_criteria>
    - `examples/clients/SMOKE_CHECKLIST.md` contains `Claude Code`, `Codex`, `Cursor`, `OpenCode`, and `Antigravity`
    - `examples/clients/SMOKE_CHECKLIST.md` contains `self_test`
    - `examples/clients/SMOKE_CHECKLIST.md` contains `Tier 2`
    - The checklist references the checked-in example config files under `examples/clients/`
  </acceptance_criteria>
  <done>The repo includes a concrete acceptance path for each target client, with honest Tier 1 versus Tier 2 labeling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "turbo-memory-mcp serve|tqmemory" README.md examples/clients/*` finds the canonical runtime and server id
- [ ] All JSON examples parse with `python -m json.tool`
- [ ] The Codex TOML example parses with `tomllib`
- [ ] `examples/clients/SMOKE_CHECKLIST.md` covers all five target clients and references `self_test`
</verification>

<success_criteria>

- All tasks completed
- README onboarding matches the real server/package contract
- Example configs exist for all five target clients
- A per-client smoke checklist exists alongside the example configs
- Tier 1 versus Tier 2 support is explicit and technically honest
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-integration-foundation/01-02-SUMMARY.md`
</output>
