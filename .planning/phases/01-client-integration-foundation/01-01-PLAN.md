---
phase: 01-client-integration-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/turbo_memory_mcp/__init__.py
  - src/turbo_memory_mcp/__main__.py
  - src/turbo_memory_mcp/cli.py
  - src/turbo_memory_mcp/server.py
  - src/turbo_memory_mcp/contracts.py
  - tests/test_cli.py
  - tests/test_tools.py
autonomous: true
requirements: [INT-01, INT-02]
user_setup: []
must_haves:
  truths:
    - "`uv run turbo-memory-mcp serve` starts a local stdio MCP server without any external service dependency."
    - "The server exposes exactly four Phase 1 tools: `health`, `server_info`, `list_scopes`, and `self_test`."
  artifacts:
    - "pyproject.toml"
    - "src/turbo_memory_mcp/cli.py"
    - "src/turbo_memory_mcp/server.py"
    - "tests/test_cli.py"
    - "tests/test_tools.py"
  key_links:
    - "`pyproject.toml` registers the console script `turbo-memory-mcp` to `turbo_memory_mcp.cli:main`."
    - "`src/turbo_memory_mcp/cli.py` routes `serve` to the stdio MCP server implementation in `src/turbo_memory_mcp/server.py`."
    - "The test suite asserts the same tool names and runtime contract that later plans will document."
---

<objective>
Create the installable Python package and the Phase 1 stdio MCP server contract.

Purpose: prove the local runtime, packaging, and minimal tool surface before any client-specific documentation or smoke workflows are added.
Output: packaged `turbo-memory-mcp` CLI, stdio MCP server, and a local pytest safety net.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-integration-foundation/01-CONTEXT.md
@.planning/phases/01-client-integration-foundation/01-RESEARCH.md
@.planning/phases/01-client-integration-foundation/01-VALIDATION.md
@TECHNICAL_SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Python package and CLI contract</name>
  <files>pyproject.toml, src/turbo_memory_mcp/__init__.py, src/turbo_memory_mcp/__main__.py, src/turbo_memory_mcp/cli.py</files>
  <read_first>.planning/phases/01-client-integration-foundation/01-CONTEXT.md, .planning/phases/01-client-integration-foundation/01-RESEARCH.md, TECHNICAL_SPEC.md, AGENTS.md</read_first>
  <action>Create `pyproject.toml` as the canonical package contract using a lightweight build backend such as `hatchling`, set `name = "turbo-memory-mcp"`, set `requires-python = ">=3.11"`, add the official `mcp` Python SDK as the MCP dependency, and register the console script `turbo-memory-mcp = turbo_memory_mcp.cli:main`. Add `src/turbo_memory_mcp/__init__.py` with a version constant, add `src/turbo_memory_mcp/__main__.py` that delegates to `main()`, and implement `src/turbo_memory_mcp/cli.py` with stdlib `argparse` only. The CLI must support `serve` and `--version`, and the help text must show the blessed runtime form `turbo-memory-mcp serve`. Do not add a web server, HTTP transport, vector store, or any optional runtime service.</action>
  <verify>uv run python -m turbo_memory_mcp --help</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "turbo-memory-mcp"` and `requires-python = ">=3.11"`
    - `pyproject.toml` registers `turbo-memory-mcp = turbo_memory_mcp.cli:main`
    - `src/turbo_memory_mcp/cli.py` contains `serve` and `--version`
    - `uv run python -m turbo_memory_mcp --help` exits 0
  </acceptance_criteria>
  <done>The project is installable as `turbo-memory-mcp`, and the CLI contract for `serve` is visible and executable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Phase 1 stdio MCP server and tool contract</name>
  <files>src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/contracts.py</files>
  <read_first>pyproject.toml, src/turbo_memory_mcp/cli.py, .planning/phases/01-client-integration-foundation/01-RESEARCH.md, TECHNICAL_SPEC.md</read_first>
  <action>Implement the server with the high-level `MCPServer` API from the official Python SDK and run it via `mcp.run(transport="stdio")`. Register exactly four tools named `health`, `server_info`, `list_scopes`, and `self_test`. Put stable payload builders in `src/turbo_memory_mcp/contracts.py` so the CLI, tests, smoke script, and docs can reuse the same shapes. `health` must return `status = "ok"`, `transport = "stdio"`, and `server_id = "tqmemory"`. `server_info` must return the product name `Turbo Quant Memory for AI Agents`, package name `turbo-memory-mcp`, runtime command `turbo-memory-mcp serve`, install contract with `uv` primary and `pip` fallback, plus supported client tiers. `list_scopes` must return the reserved scope names `project`, `global`, and `hybrid` with a status such as `planned` and a note that real storage arrives in later phases. `self_test` must return `status = "ok"`, `tool_count = 4`, `tool_names = ["health","server_info","list_scopes","self_test"]`, and echo the same runtime/client contract values. Keep everything offline and deterministic.</action>
  <verify>uv run pytest -q tests/test_tools.py</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/server.py` contains the tool names `health`, `server_info`, `list_scopes`, and `self_test`
    - `src/turbo_memory_mcp/contracts.py` contains `Turbo Quant Memory for AI Agents` and `turbo-memory-mcp serve`
    - `server_info` returns Tier 1 clients `Claude Code`, `Codex`, `Cursor`, and `OpenCode`
    - `list_scopes` returns `project`, `global`, and `hybrid`
  </acceptance_criteria>
  <done>The stdio server exports the agreed four-tool Phase 1 contract without implying any real memory storage behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add the foundational pytest safety net</name>
  <files>tests/test_cli.py, tests/test_tools.py, pyproject.toml</files>
  <read_first>pyproject.toml, src/turbo_memory_mcp/cli.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/contracts.py</read_first>
  <action>Add a pytest-based local test suite that covers the packaging contract and the tool payload contract. `tests/test_cli.py` must assert the CLI exposes `serve` and version/help behavior. `tests/test_tools.py` must assert the exact tool names, `server_info` payload fields, reserved scopes, and the `self_test` tool summary. If pytest configuration is needed, place it inside `pyproject.toml` so `uv run pytest -q` becomes the canonical quick-run command. Do not rely on network access, external services, or GUI clients in these tests.</action>
  <verify>uv run pytest -q</verify>
  <acceptance_criteria>
    - `tests/test_cli.py` exists and references `serve`
    - `tests/test_tools.py` exists and references all four Phase 1 tool names
    - `pyproject.toml` supports running `uv run pytest -q`
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>The repo has a fast local automated loop that protects the package and tool contract before client docs or smoke flows are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run python -m turbo_memory_mcp --help` succeeds
- [ ] `uv run pytest -q` succeeds
- [ ] The tool catalog is exactly `health`, `server_info`, `list_scopes`, `self_test`
</verification>

<success_criteria>

- All tasks completed
- The package can be launched locally over stdio with `uv run turbo-memory-mcp serve`
- The server exposes the four agreed Phase 1 tools and nothing broader
- The test suite provides a stable offline quick-check for the package and tool contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-integration-foundation/01-01-SUMMARY.md`
</output>
