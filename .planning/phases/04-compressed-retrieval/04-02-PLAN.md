---
phase: 04-compressed-retrieval
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/turbo_memory_mcp/contracts.py
  - src/turbo_memory_mcp/retrieval.py
  - src/turbo_memory_mcp/server.py
  - tests/test_semantic_search.py
  - tests/test_namespace_tools.py
autonomous: true
requirements: [RET-01, RET-02, SAFE-01, SAFE-02]
user_setup: []
must_haves:
  truths:
    - "The only public retrieval tool in Phase 4 is `semantic_search(...)`."
    - "Search covers both Markdown blocks and memory notes while preserving project bias and markdown-first tie-breaks."
    - "Default results are balanced cards with provenance, `compressed_summary`, and at most `2-3` `key_points`, without raw excerpts."
  artifacts:
    - "src/turbo_memory_mcp/retrieval.py"
    - "src/turbo_memory_mcp/contracts.py"
    - "src/turbo_memory_mcp/server.py"
    - "tests/test_semantic_search.py"
  key_links:
    - "`src/turbo_memory_mcp/retrieval.py` owns query orchestration, ranking, confidence states, and deterministic compression."
    - "`src/turbo_memory_mcp/server.py` replaces the public `search_memory` MCP tool with `semantic_search` and wires retrieval sync into live write/index paths."
    - "`tests/test_semantic_search.py` and `tests/test_namespace_tools.py` protect scope behavior, markdown-first ordering, and low-confidence signaling."
---

<objective>
Implement the live semantic retrieval contract for Phase 4.

Purpose: expose one canonical `semantic_search(...)` tool that returns compact, provenance-rich balanced cards over both stored Markdown blocks and persistent notes.
Output: retrieval orchestration, Phase 4 tool contract updates, and focused tests for ranking, compression, and warnings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compressed-retrieval/04-CONTEXT.md
@.planning/phases/04-compressed-retrieval/04-RESEARCH.md
@.planning/phases/04-compressed-retrieval/04-VALIDATION.md
@src/turbo_memory_mcp/contracts.py
@src/turbo_memory_mcp/server.py
@src/turbo_memory_mcp/store.py
@src/turbo_memory_mcp/retrieval_index.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement semantic retrieval orchestration and balanced-card compression</name>
  <files>src/turbo_memory_mcp/retrieval.py, src/turbo_memory_mcp/contracts.py</files>
  <read_first>.planning/phases/04-compressed-retrieval/04-CONTEXT.md, .planning/phases/04-compressed-retrieval/04-RESEARCH.md, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/retrieval_index.py, src/turbo_memory_mcp/store.py</read_first>
  <action>Create `src/turbo_memory_mcp/retrieval.py` with a `semantic_search` orchestration layer that queries the embedded retrieval index by `project`, `global`, or `hybrid` scope, merges hits using the locked Phase 2 precedence, and enforces markdown-first tie-breaks inside each scope before falling back to newer `updated_at` and stable identity. Build deterministic balanced-card shaping for both blocks and notes: return `scope`, `project_id`, `project_name`, `source_kind`, `item_id`, `block_id` when applicable, `source_path`, `heading_path` or `title`, `score`, `confidence`, `confidence_state`, `compressed_summary`, `key_points`, `can_hydrate`, and `promoted_from` when relevant. Generate `compressed_summary` and `key_points` structurally from stored text and metadata, keep `key_points` capped at `2-3`, and do not return raw excerpts by default.</action>
  <verify>uv run pytest -q tests/test_semantic_search.py -k "project_scope or balanced_card or no_raw_excerpt"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/retrieval.py` contains `semantic_search`
    - `src/turbo_memory_mcp/retrieval.py` contains `confidence_state`
    - `src/turbo_memory_mcp/retrieval.py` contains `compressed_summary`
    - `src/turbo_memory_mcp/retrieval.py` contains `key_points`
    - `src/turbo_memory_mcp/retrieval.py` contains `markdown-first`
    - `src/turbo_memory_mcp/contracts.py` contains `compressed_summary`
  </acceptance_criteria>
  <done>The repo has a deterministic semantic-retrieval layer that emits high-signal balanced cards instead of raw content dumps.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the public retrieval tool contract and wire retrieval sync boundaries</name>
  <files>src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py</files>
  <read_first>src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/retrieval.py, src/turbo_memory_mcp/retrieval_index.py, .planning/phases/04-compressed-retrieval/04-CONTEXT.md</read_first>
  <action>Update `src/turbo_memory_mcp/contracts.py` so the exported tool catalog becomes Phase 4 aware: `semantic_search` replaces `search_memory` in the public tool names, `self_test` reflects the new tool list, and the public search payload exposes result-level warning state without raw excerpts. Update `src/turbo_memory_mcp/server.py` so the MCP server exports `semantic_search(query: str, scope: str = \"hybrid\", limit: int = 5)` as the only public retrieval tool, removes `search_memory` from the live tool registration, and keeps retrieval rows fresh by syncing note changes after `remember_note` and `promote_note` and syncing Markdown rows after `index_paths` runs. Preserve the current project/global/hybrid validation rules and fail-fast behavior for empty queries or unsupported scopes.</action>
  <verify>uv run pytest -q tests/test_semantic_search.py tests/test_namespace_tools.py -k "semantic_search or hybrid or low_confidence"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/contracts.py` contains `PHASE_4_TOOL_NAMES`
    - `src/turbo_memory_mcp/contracts.py` contains `semantic_search`
    - `src/turbo_memory_mcp/server.py` contains `def semantic_search(`
    - `src/turbo_memory_mcp/server.py` contains `semantic_search_impl`
    - `src/turbo_memory_mcp/server.py` contains `remember_note_impl`
    - `src/turbo_memory_mcp/server.py` contains `index_paths_impl`
  </acceptance_criteria>
  <done>The live MCP tool surface now exposes `semantic_search(...)` and keeps retrieval data synchronized with note writes and Markdown indexing.</done>
</task>

<task type="auto">
  <name>Task 3: Lock ranking, warning, and provenance behavior with focused tests</name>
  <files>tests/test_semantic_search.py, tests/test_namespace_tools.py</files>
  <read_first>.planning/phases/04-compressed-retrieval/04-VALIDATION.md, src/turbo_memory_mcp/retrieval.py, src/turbo_memory_mcp/server.py, tests/test_namespace_tools.py, AGENTS.md</read_first>
  <action>Create `tests/test_semantic_search.py` with hermetic cases for: project-only retrieval, global-only retrieval, hybrid project bias, markdown-first ordering when notes and source blocks both match, explicit `confidence_state` / `warning` behavior for weak or ambiguous matches, and the default balanced-card contract without raw excerpt fields. Update `tests/test_namespace_tools.py` so it imports `semantic_search_impl` instead of `search_memory_impl` and covers note participation plus `promoted_from` provenance in global results. Use grep-verifiable fixture phrases such as `auth refresh rotation`, `session cache`, and `ambiguous token`.</action>
  <verify>uv run pytest -q tests/test_semantic_search.py tests/test_namespace_tools.py</verify>
  <acceptance_criteria>
    - `tests/test_semantic_search.py` contains `auth refresh rotation`
    - `tests/test_semantic_search.py` contains `confidence_state`
    - `tests/test_semantic_search.py` contains `compressed_summary`
    - `tests/test_semantic_search.py` contains `key_points`
    - `tests/test_namespace_tools.py` contains `semantic_search_impl`
    - `uv run pytest -q tests/test_semantic_search.py tests/test_namespace_tools.py` exits 0
  </acceptance_criteria>
  <done>Automated tests now protect the Phase 4 retrieval contract, ranking rules, and warning envelope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_semantic_search.py tests/test_namespace_tools.py` succeeds
- [ ] The live MCP server exports `semantic_search` and no longer publicly exports `search_memory`
- [ ] Default results are balanced cards with provenance and no raw excerpt field
</verification>

<success_criteria>

- All tasks completed
- `semantic_search(...)` is the only public retrieval API for Phase 4
- Search covers Markdown blocks and notes across `project`, `global`, and `hybrid`
- Ranking preserves project bias, markdown-first ordering, and explicit warning states
</success_criteria>

<output>
After completion, create `.planning/phases/04-compressed-retrieval/04-02-SUMMARY.md`
</output>
