---
phase: 04-compressed-retrieval
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/turbo_memory_mcp/store.py
  - src/turbo_memory_mcp/retrieval_index.py
  - tests/test_retrieval_index.py
autonomous: true
requirements: [RET-01, SAFE-01]
user_setup: []
must_haves:
  truths:
    - "A derived retrieval index exists under the existing local storage root without replacing JSON note/block records as the source of truth."
    - "Markdown blocks and memory notes can be mirrored into scope-aware retrieval rows with explicit provenance fields."
    - "Automated tests prove sync and stale-row cleanup for both project and global retrieval data."
  artifacts:
    - "pyproject.toml"
    - "src/turbo_memory_mcp/retrieval_index.py"
    - "tests/test_retrieval_index.py"
  key_links:
    - "`src/turbo_memory_mcp/retrieval_index.py` owns the embedded retrieval tables, embedding model access, and row-sync helpers."
    - "`src/turbo_memory_mcp/store.py` remains the canonical JSON store and only gains the minimum helpers needed to mirror notes and Markdown blocks."
    - "`tests/test_retrieval_index.py` locks the project/global retrieval layout and sync behavior before the MCP tool contract depends on it."
---

<objective>
Create the embedded retrieval-index foundation for Phase 4.

Purpose: add a local vector-backed mirror of stored notes and Markdown blocks so semantic retrieval can be layered on top of the existing JSON store instead of replacing it.
Output: retrieval dependencies, retrieval-index storage/sync helpers, and hermetic tests for project/global mirroring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compressed-retrieval/04-CONTEXT.md
@.planning/phases/04-compressed-retrieval/04-RESEARCH.md
@.planning/phases/04-compressed-retrieval/04-VALIDATION.md
@pyproject.toml
@src/turbo_memory_mcp/store.py
@src/turbo_memory_mcp/ingestion.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the retrieval dependencies and embedded index scaffold</name>
  <files>pyproject.toml, src/turbo_memory_mcp/retrieval_index.py</files>
  <read_first>.planning/phases/04-compressed-retrieval/04-RESEARCH.md, pyproject.toml, src/turbo_memory_mcp/store.py, AGENTS.md</read_first>
  <action>Update `pyproject.toml` to add `lancedb>=0.30.1,<0.31` and `sentence-transformers>=5.3.0,<6.0` as the only new Phase 4 runtime dependencies. Create `src/turbo_memory_mcp/retrieval_index.py` with a narrow embedded-index API that connects to a file-backed LanceDB directory under `projects/<project_id>/retrieval/` for project data and `global/retrieval/` for global data. Load embeddings through `SentenceTransformer("all-MiniLM-L6-v2")`. Define the exact row shape needed later by semantic retrieval: `scope`, `project_id`, `project_name`, `source_kind`, `item_id`, `block_id`, `note_id`, `source_path`, `heading_path`, `title`, `tags`, `content_search`, `content_summary_seed`, `updated_at`, and `vector`.</action>
  <verify>uv run pytest -q tests/test_retrieval_index.py -k "layout or model"</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `lancedb`
    - `pyproject.toml` contains `sentence-transformers`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `SentenceTransformer("all-MiniLM-L6-v2")`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `projects/<project_id>/retrieval/`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `global/retrieval/`
  </acceptance_criteria>
  <done>The repo has an embedded retrieval-index module and the minimum dependencies required to power semantic lookup locally.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror Markdown blocks and notes into scope-aware retrieval rows</name>
  <files>src/turbo_memory_mcp/store.py, src/turbo_memory_mcp/retrieval_index.py</files>
  <read_first>src/turbo_memory_mcp/store.py, src/turbo_memory_mcp/retrieval_index.py, .planning/phases/04-compressed-retrieval/04-RESEARCH.md, .planning/phases/03-markdown-ingestion/03-03-SUMMARY.md</read_first>
  <action>Implement deterministic adapters in `src/turbo_memory_mcp/retrieval_index.py` that convert existing `MemoryStore` records into retrieval rows for both `source_kind = "markdown"` and `source_kind = "memory_note"`. Add scope-specific sync helpers that can rebuild or refresh the project table from `list_markdown_blocks()` plus `list_notes("project")`, and the global table from `list_notes("global")`, while pruning stale rows for deleted blocks or deleted notes. Keep JSON files in `src/turbo_memory_mcp/store.py` as the only durable source data; any store changes in this task must be helper-level only and must not introduce a second canonical store.</action>
  <verify>uv run pytest -q tests/test_retrieval_index.py -k "sync or stale"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/retrieval_index.py` contains `source_kind`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `memory_note`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `markdown`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `sync_project`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `sync_global`
    - `src/turbo_memory_mcp/retrieval_index.py` contains `stale`
  </acceptance_criteria>
  <done>The retrieval layer can now mirror the current store into project/global retrieval tables without changing the canonical persistence model.</done>
</task>

<task type="auto">
  <name>Task 3: Add hermetic tests for retrieval-index sync and cleanup</name>
  <files>tests/test_retrieval_index.py</files>
  <read_first>.planning/phases/04-compressed-retrieval/04-VALIDATION.md, src/turbo_memory_mcp/store.py, src/turbo_memory_mcp/retrieval_index.py, tests/test_ingestion_store.py, AGENTS.md</read_first>
  <action>Create `tests/test_retrieval_index.py` with temp-home coverage driven by `TQMEMORY_HOME`, `TQMEMORY_PROJECT_ID`, and `TQMEMORY_PROJECT_NAME`. Seed project notes, promoted global notes, and indexed Markdown block records, then assert that retrieval sync writes rows for both source kinds, preserves `scope`, `project_id`, `source_path`, `block_id`, and `note_id`, rewrites rows after content changes, and removes stale rows after block or note deletion. Use grep-verifiable fixture values such as `auth-architecture`, `global-pattern-note`, and `docs/auth.md`.</action>
  <verify>uv run pytest -q tests/test_retrieval_index.py</verify>
  <acceptance_criteria>
    - `tests/test_retrieval_index.py` contains `auth-architecture`
    - `tests/test_retrieval_index.py` contains `global-pattern-note`
    - `tests/test_retrieval_index.py` contains `docs/auth.md`
    - `tests/test_retrieval_index.py` contains `source_kind`
    - `tests/test_retrieval_index.py` contains `block_id`
    - `uv run pytest -q tests/test_retrieval_index.py` exits 0
  </acceptance_criteria>
  <done>The retrieval-index foundation is protected by automated sync and cleanup tests before live semantic search depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_retrieval_index.py` succeeds
- [ ] Retrieval data lives under the existing local storage root rather than a separate service
- [ ] Project and global retrieval rows can be refreshed without replacing JSON source records
</verification>

<success_criteria>

- All tasks completed
- A file-backed retrieval index mirrors stored notes and Markdown blocks
- Project and global scope data stay distinct in the retrieval layer
- Automated tests prove sync and stale-row cleanup for both source kinds
</success_criteria>

<output>
After completion, create `.planning/phases/04-compressed-retrieval/04-01-SUMMARY.md`
</output>
