---
phase: 04-compressed-retrieval
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - README.md
  - TECHNICAL_SPEC.md
  - MEMORY_STRATEGY.md
  - examples/clients/SMOKE_CHECKLIST.md
  - tests/test_tools.py
  - tests/test_smoke_contract.py
  - scripts/smoke_test.py
autonomous: true
requirements: [RET-01, RET-02, SAFE-01, SAFE-02]
user_setup: []
must_haves:
  truths:
    - "Published docs and smoke guidance describe `semantic_search(...)` as the Phase 4 retrieval path."
    - "Tool-catalog and smoke-contract tests lock the Phase 4 public API and balanced-card envelope."
    - "The stdio smoke script proves end-to-end semantic retrieval over indexed Markdown plus persistent notes without raw-file dumps."
  artifacts:
    - "README.md"
    - "tests/test_tools.py"
    - "tests/test_smoke_contract.py"
    - "scripts/smoke_test.py"
  key_links:
    - "`README.md`, `TECHNICAL_SPEC.md`, and `MEMORY_STRATEGY.md` explain how compressed retrieval works and where hydration is deferred to Phase 5."
    - "`tests/test_tools.py` and `tests/test_smoke_contract.py` freeze the published MCP contract around `semantic_search`."
    - "`scripts/smoke_test.py` exercises the real stdio server and proves the retrieval contract end-to-end."
---

<objective>
Publish and verify the Phase 4 retrieval contract end to end.

Purpose: update the operator-facing docs, published MCP contract tests, and real stdio smoke flow so adoption material matches the new semantic retrieval behavior.
Output: refreshed docs, contract tests, and a real smoke path for `semantic_search(...)`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compressed-retrieval/04-CONTEXT.md
@.planning/phases/04-compressed-retrieval/04-RESEARCH.md
@.planning/phases/04-compressed-retrieval/04-VALIDATION.md
@README.md
@TECHNICAL_SPEC.md
@MEMORY_STRATEGY.md
@tests/test_tools.py
@tests/test_smoke_contract.py
@scripts/smoke_test.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs and operator guidance for semantic retrieval</name>
  <files>README.md, TECHNICAL_SPEC.md, MEMORY_STRATEGY.md, examples/clients/SMOKE_CHECKLIST.md</files>
  <read_first>README.md, TECHNICAL_SPEC.md, MEMORY_STRATEGY.md, .planning/phases/04-compressed-retrieval/04-CONTEXT.md, .planning/phases/04-compressed-retrieval/04-RESEARCH.md, AGENTS.md</read_first>
  <action>Update the user-facing docs so Phase 4 is explained consistently: `semantic_search(...)` is now the canonical retrieval tool, it searches both Markdown blocks and memory notes, it returns balanced cards with `compressed_summary` and up to `2-3` `key_points`, and it does not return raw excerpts or whole files by default. Document the locked ranking rules (`project` bias plus markdown-first inside each scope), the explicit low-confidence warning behavior, and the fact that fuller hydration is deferred to Phase 5. Refresh `examples/clients/SMOKE_CHECKLIST.md` so the manual smoke flow names `semantic_search` instead of `search_memory`.</action>
  <verify>rg -n "semantic_search|compressed_summary|key_points|Phase 5|search_memory" README.md TECHNICAL_SPEC.md MEMORY_STRATEGY.md examples/clients/SMOKE_CHECKLIST.md</verify>
  <acceptance_criteria>
    - `README.md` contains `semantic_search`
    - `TECHNICAL_SPEC.md` contains `compressed_summary`
    - `MEMORY_STRATEGY.md` contains `markdown-first`
    - `examples/clients/SMOKE_CHECKLIST.md` contains `semantic_search`
    - `README.md` contains `Phase 5`
  </acceptance_criteria>
  <done>The published docs now describe the real Phase 4 retrieval contract and its boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Update the published tool and smoke-contract tests for Phase 4</name>
  <files>tests/test_tools.py, tests/test_smoke_contract.py</files>
  <read_first>.planning/phases/04-compressed-retrieval/04-VALIDATION.md, tests/test_tools.py, tests/test_smoke_contract.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py</read_first>
  <action>Update `tests/test_tools.py` so the expected live tool catalog and `self_test` payload expose `semantic_search` in the Phase 4 tool list and no longer expect `search_memory`. Update `tests/test_smoke_contract.py` so the published contract asserts the balanced-card retrieval fields `source_kind`, `block_id`, `compressed_summary`, `key_points`, `confidence_state`, and scope/project provenance, while also asserting that the default payload does not include a raw excerpt field. Keep these tests narrow and contract-focused rather than re-testing ranking internals.</action>
  <verify>uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py</verify>
  <acceptance_criteria>
    - `tests/test_tools.py` contains `semantic_search`
    - `tests/test_tools.py` contains `PHASE_4_TOOL_NAMES`
    - `tests/test_smoke_contract.py` contains `compressed_summary`
    - `tests/test_smoke_contract.py` contains `confidence_state`
    - `tests/test_smoke_contract.py` contains `block_id`
    - `uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py` exits 0
  </acceptance_criteria>
  <done>The published contract tests now freeze the Phase 4 public API and balanced-card envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the real stdio smoke path for semantic retrieval</name>
  <files>scripts/smoke_test.py</files>
  <read_first>scripts/smoke_test.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/retrieval.py, tests/test_semantic_search.py, README.md</read_first>
  <action>Update `scripts/smoke_test.py` so the real stdio flow indexes temporary Markdown documents, writes and promotes at least one note, and then calls `semantic_search(...)` instead of `search_memory(...)`. Assert that the returned top hits include Phase 4 balanced-card fields, preserve provenance back to the source block or note, do not include a raw excerpt field, and still show project-first ordering when project and global hits are both relevant. Keep the smoke output compact and pass/fail oriented, but make the PASS lines explicitly mention `semantic_search`.</action>
  <verify>uv run python scripts/smoke_test.py</verify>
  <acceptance_criteria>
    - `scripts/smoke_test.py` contains `semantic_search`
    - `scripts/smoke_test.py` contains `compressed_summary`
    - `scripts/smoke_test.py` contains `confidence_state`
    - `scripts/smoke_test.py` contains `block_id`
    - `scripts/smoke_test.py` contains `raw excerpt`
    - `uv run python scripts/smoke_test.py` exits 0
  </acceptance_criteria>
  <done>The repo smoke path now proves end-to-end semantic retrieval over the real stdio MCP server.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py` succeeds
- [ ] `uv run python scripts/smoke_test.py` succeeds
- [ ] Docs and smoke guidance reference `semantic_search` rather than the removed public `search_memory` tool
</verification>

<success_criteria>

- All tasks completed
- Published docs describe the Phase 4 retrieval contract accurately
- Contract tests freeze the Phase 4 tool surface and balanced-card payload
- The stdio smoke path proves semantic retrieval over indexed Markdown and notes
</success_criteria>

<output>
After completion, create `.planning/phases/04-compressed-retrieval/04-03-SUMMARY.md`
</output>
