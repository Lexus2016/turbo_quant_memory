---
phase: 02-namespace-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turbo_memory_mcp/identity.py
  - src/turbo_memory_mcp/store.py
  - tests/test_identity.py
  - tests/test_storage.py
autonomous: true
requirements: [SCP-01, SCP-02]
user_setup: []
must_haves:
  truths:
    - "The current repository resolves to a deterministic `project_id` using normalized git remote URL first, with repo-root-path hash fallback."
    - "A central local store exists under `~/.turbo-quant-memory/` with deterministic `projects/<project_id>/...` and `global/...` partitions."
    - "Project and global notes/manifests can be written atomically without external services."
  artifacts:
    - "src/turbo_memory_mcp/identity.py"
    - "src/turbo_memory_mcp/store.py"
    - "tests/test_identity.py"
    - "tests/test_storage.py"
  key_links:
    - "`src/turbo_memory_mcp/identity.py` resolves repo root, normalized remote identity, and explicit override inputs."
    - "`src/turbo_memory_mcp/store.py` owns the central storage layout and atomic note/manifest persistence primitives."
    - "The tests prove remote-first identity, no-remote fallback, and central-store persistence."
---

<objective>
Create the deterministic namespace foundation for Phase 2.

Purpose: give later server tools a stable current-project identity and a real central storage layout for project and global scopes.
Output: identity resolver, atomic local store primitives, and focused unit coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-namespace-model/02-CONTEXT.md
@.planning/phases/02-namespace-model/02-RESEARCH.md
@.planning/phases/02-namespace-model/02-VALIDATION.md
@MEMORY_STRATEGY.md
@TECHNICAL_SPEC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic project identity resolution</name>
  <files>src/turbo_memory_mcp/identity.py, tests/test_identity.py</files>
  <read_first>.planning/phases/02-namespace-model/02-CONTEXT.md, .planning/phases/02-namespace-model/02-RESEARCH.md, AGENTS.md, src/turbo_memory_mcp/cli.py, src/turbo_memory_mcp/server.py</read_first>
  <action>Create `src/turbo_memory_mcp/identity.py` with a small stdlib-only resolver for current-project identity. Implement concrete support for `TQMEMORY_PROJECT_ROOT`, `TQMEMORY_PROJECT_ID`, and `TQMEMORY_PROJECT_NAME` environment overrides. Without overrides, resolve the working tree root with `git rev-parse --show-toplevel`; resolve the primary remote with `git remote get-url origin`; if no remote exists, fall back to the resolved repo root path. Normalize remote identity by trimming whitespace, lowercasing the host portion, converting SCP-style `git@host:owner/repo.git` into a canonical host/path form, and stripping a trailing `.git`. Derive `project_id` as the first 16 hex characters of `sha256(canonical_identity_source)`. Set `project_name` to the override when present, otherwise use the repo-root directory name. Add `tests/test_identity.py` covering: remote-first identity, no-remote fallback, and explicit override precedence.</action>
  <verify>uv run pytest -q tests/test_identity.py</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/identity.py` contains `git rev-parse --show-toplevel`
    - `src/turbo_memory_mcp/identity.py` contains `git remote get-url origin`
    - `src/turbo_memory_mcp/identity.py` contains `TQMEMORY_PROJECT_ID`
    - `tests/test_identity.py` exists and references remote-first, fallback, and override cases
    - `uv run pytest -q tests/test_identity.py` exits 0
  </acceptance_criteria>
  <done>The repo can deterministically resolve `project_id` and `project_name` for the current repository without adding a new Git dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Build the central namespace store and atomic persistence primitives</name>
  <files>src/turbo_memory_mcp/store.py, tests/test_storage.py</files>
  <read_first>.planning/phases/02-namespace-model/02-CONTEXT.md, .planning/phases/02-namespace-model/02-RESEARCH.md, src/turbo_memory_mcp/identity.py, MEMORY_STRATEGY.md, TECHNICAL_SPEC.md</read_first>
  <action>Create `src/turbo_memory_mcp/store.py` for central local storage under `~/.turbo-quant-memory/`, with `TQMEMORY_HOME` as an optional override for tests and advanced local setups. Implement exact layout helpers for `projects/<project_id>/manifest.json`, `projects/<project_id>/notes/<note_id>.json`, `global/manifest.json`, and `global/notes/<note_id>.json`. Define a concrete note record schema containing `note_id`, `scope`, `project_id`, `project_name`, `title`, `content`, `tags`, `source_refs`, `source_kind = "memory_note"`, `created_at`, `updated_at`, and optional `promoted_from`. Implement atomic JSON writes in the target directory using `tempfile.NamedTemporaryFile(delete=False)` plus `os.replace()`. Add helper methods that ensure the directory tree exists, write a project manifest, write a project note, write a global note, and promote a note into global while preserving `promoted_from`. Add `tests/test_storage.py` covering central-root layout, atomic write helpers, manifest persistence, and promotion lineage fields.</action>
  <verify>uv run pytest -q tests/test_storage.py</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/store.py` contains `.turbo-quant-memory`
    - `src/turbo_memory_mcp/store.py` contains `NamedTemporaryFile`
    - `src/turbo_memory_mcp/store.py` contains `os.replace`
    - `src/turbo_memory_mcp/store.py` contains `promoted_from`
    - `tests/test_storage.py` exists and references `projects/` and `global/`
    - `uv run pytest -q tests/test_storage.py` exits 0
  </acceptance_criteria>
  <done>The repo has a real central store layout and atomic persistence primitives for `project` and `global` namespace data.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the foundation with focused namespace unit coverage</name>
  <files>tests/test_identity.py, tests/test_storage.py</files>
  <read_first>src/turbo_memory_mcp/identity.py, src/turbo_memory_mcp/store.py, .planning/phases/02-namespace-model/02-VALIDATION.md</read_first>
  <action>Expand the new test files so they assert stable behavior rather than only file existence. The identity tests must assert that the same canonical remote string produces the same 16-character `project_id`, that path fallback still produces a non-empty deterministic ID, and that explicit overrides bypass git discovery. The storage tests must assert that project notes and global notes persist under the exact central layout, that written JSON includes `scope`, `project_id`, and `project_name`, and that promotion preserves `promoted_from` instead of losing the original namespace origin.</action>
  <verify>uv run pytest -q tests/test_identity.py tests/test_storage.py</verify>
  <acceptance_criteria>
    - `tests/test_identity.py` contains `project_id`
    - `tests/test_storage.py` contains `promoted_from`
    - `tests/test_storage.py` contains `scope`
    - `uv run pytest -q tests/test_identity.py tests/test_storage.py` exits 0
  </acceptance_criteria>
  <done>The Phase 2 foundation has automated checks for deterministic identity and safe namespace persistence before MCP tools depend on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_identity.py` succeeds
- [ ] `uv run pytest -q tests/test_storage.py` succeeds
- [ ] The central storage layout is exactly `projects/<project_id>/...` plus `global/...`
</verification>

<success_criteria>

- All tasks completed
- Deterministic project identity exists with remote-first fallback logic
- A central namespace store exists under `~/.turbo-quant-memory/`
- Atomic persistence primitives exist for notes and manifests
- Unit coverage protects the new identity and storage behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-namespace-model/02-01-SUMMARY.md`
</output>
