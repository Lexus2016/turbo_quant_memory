---
phase: 02-namespace-model
plan: "01"
subsystem: storage
tags: [python, git, filesystem, namespace, pytest]
requires:
  - "01-01 packaged stdio MCP server contract"
provides:
  - "Deterministic project identity with git-remote-first fallback"
  - "Central ~/.turbo-quant-memory namespace layout for project and global notes"
  - "Atomic manifest and note persistence primitives with promotion lineage"
affects: ["02-02", "02-03", "namespace tools", "smoke validation"]
tech-stack:
  added: []
  patterns:
    - "Project identity is resolved through Git CLI and explicit environment overrides"
    - "Namespace data persists as atomic JSON files under a central local store"
key-files:
  created:
    - src/turbo_memory_mcp/identity.py
    - src/turbo_memory_mcp/store.py
    - tests/test_identity.py
    - tests/test_storage.py
  modified:
    - .gitignore
key-decisions:
  - "Used normalized origin remote URLs as the primary identity source, with repo-root hashing as the no-remote fallback."
  - "Kept Phase 2 storage dependency-light by using JSON files plus NamedTemporaryFile and os.replace instead of adding a database early."
  - "Preserved origin provenance in promoted global notes through a compact promoted_from envelope."
patterns-established:
  - "Namespace foundations should be hermetic and testable through environment overrides such as TQMEMORY_PROJECT_ID and TQMEMORY_HOME."
  - "Promotion into global scope must preserve project origin instead of rewriting the note as anonymous shared state."
requirements-completed: [SCP-01, SCP-02]
duration: 2min
completed: 2026-03-25
---

# Phase 02 Plan 01: Namespace Model Summary

**Deterministic repository identity plus a central local namespace store for project and global note persistence**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-25T21:13:54Z
- **Completed:** 2026-03-25T21:15:46Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Added `src/turbo_memory_mcp/identity.py` with remote-first project detection, path fallback, and explicit environment overrides.
- Added `src/turbo_memory_mcp/store.py` with the exact `projects/<project_id>/...` and `global/...` layout plus atomic JSON persistence.
- Added hermetic pytest coverage for identity stability, manifest writes, note persistence, and promotion lineage.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build deterministic project identity resolution** - `1dbf98e` (`feat`)
2. **Task 2: Build the central namespace store and atomic persistence primitives** - `0ce9532` (`feat`)
3. **Task 3: Lock the foundation with focused namespace unit coverage** - `65f84a4` (`test`)

## Files Created/Modified

- `src/turbo_memory_mcp/identity.py` - Remote normalization, repo-root fallback, and project override support.
- `src/turbo_memory_mcp/store.py` - Central storage layout helpers, atomic JSON writes, note reads/listing, and promotion lineage.
- `tests/test_identity.py` - Remote-first, no-remote, override, and canonical-hash stability coverage.
- `tests/test_storage.py` - Central layout, manifest persistence, note reads, and promoted_from coverage.
- `.gitignore` - Ignores Python bytecode noise generated during local test execution.

## Decisions Made

- Kept the identity path stdlib-only and shell-based by calling Git directly instead of introducing `GitPython`.
- Preserved the source project metadata inside global notes so later hybrid results can always show project origin.
- Added lightweight read/list helpers to the store now because Phase `02-02` needs them for namespace-aware MCP tools.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Ignored Python bytecode artefacts generated by pytest**
- **Found during:** Task 3 (Lock the foundation with focused namespace unit coverage)
- **Issue:** Running pytest created `__pycache__` noise that polluted `git status` and risked contaminating task commits.
- **Fix:** Added standard Python bytecode ignore rules to `.gitignore`.
- **Files modified:** `.gitignore`
- **Verification:** `git status --short --untracked-files=all` no longer reports cache directories after the ignore update.
- **Committed in:** plan completion metadata commit

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** No scope creep. The housekeeping fix only protected clean task commits and repeatable local execution.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `02-02` can build namespace-aware MCP tools directly on top of `resolve_project_identity()` and `MemoryStore`.
- The current store already exposes the minimum read/list/promote primitives needed for `remember_note`, `promote_note`, and `search_memory`.
- No blockers remain for the namespace contract wave.

## Self-Check: PASSED

- Verified `uv run pytest -q tests/test_identity.py tests/test_storage.py` exits 0.
- Verified the central layout uses exactly `projects/<project_id>/...` plus `global/...`.
- Verified task commits `1dbf98e`, `0ce9532`, and `65f84a4` exist in git history.
