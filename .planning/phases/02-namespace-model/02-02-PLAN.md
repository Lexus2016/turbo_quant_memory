---
phase: 02-namespace-model
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/turbo_memory_mcp/contracts.py
  - src/turbo_memory_mcp/server.py
  - tests/test_tools.py
  - tests/test_namespace_tools.py
autonomous: true
requirements: [SCP-01, SCP-02, SCP-03, SCP-04]
user_setup: []
must_haves:
  truths:
    - "Agents can write notes into `project` scope, promote notes into `global`, and query `project`, `global`, or `hybrid` through the MCP server."
    - "Hybrid search uses deterministic precedence with strong project bias and provenance-rich compact envelopes."
    - "Every returned result identifies its scope and project origin."
  artifacts:
    - "src/turbo_memory_mcp/contracts.py"
    - "src/turbo_memory_mcp/server.py"
    - "tests/test_tools.py"
    - "tests/test_namespace_tools.py"
  key_links:
    - "`src/turbo_memory_mcp/server.py` wires namespace-aware tools onto the same stdio MCP server."
    - "`src/turbo_memory_mcp/contracts.py` defines the compact provenance envelope returned to clients."
    - "The tests assert project/global/hybrid behavior and reject unsafe direct global writes."
---

<objective>
Implement the Phase 2 namespace-aware MCP contract.

Purpose: expose safe write/query behavior for project and global memory without changing the Phase 1 server transport or deployment contract.
Output: namespace-aware tools, deterministic hybrid precedence, compact provenance envelopes, and contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-namespace-model/02-CONTEXT.md
@.planning/phases/02-namespace-model/02-RESEARCH.md
@.planning/phases/02-namespace-model/02-VALIDATION.md
@README.md
@src/turbo_memory_mcp/identity.py
@src/turbo_memory_mcp/store.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the public contract for namespace-aware metadata</name>
  <files>src/turbo_memory_mcp/contracts.py</files>
  <read_first>.planning/phases/02-namespace-model/02-CONTEXT.md, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/identity.py, src/turbo_memory_mcp/store.py, README.md</read_first>
  <action>Update `src/turbo_memory_mcp/contracts.py` so the Phase 2 server contract can expose current project and namespace metadata. Keep the existing Phase 1 constants, but replace the old planned-scope placeholder with concrete builders that return compact standard envelopes. Extend `server_info` to include `storage_root`, `current_project` with `project_id` and `project_name`, and supported query modes `project`, `global`, `hybrid`. Add builders for note write and search envelopes that include exactly these default fields: `scope`, `project_id`, `project_name`, `source_kind`, `item_id` or `block_id`, `source_path`, `updated_at`, `confidence`, `can_hydrate`, plus `promoted_from` when present. Keep heavier lineage/debug data out of the default payload builders. Update `self_test` so it reports the expanded tool catalog and current namespace contract.</action>
  <verify>uv run pytest -q tests/test_tools.py -k contract</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/contracts.py` contains `storage_root`
    - `src/turbo_memory_mcp/contracts.py` contains `current_project`
    - `src/turbo_memory_mcp/contracts.py` contains `promoted_from`
    - `src/turbo_memory_mcp/contracts.py` contains `confidence`
    - `src/turbo_memory_mcp/contracts.py` contains `can_hydrate`
  </acceptance_criteria>
  <done>The public contract builders can now represent namespace-aware metadata without bloating the default response volume.</done>
</task>

<task type="auto">
  <name>Task 2: Wire namespace-aware write, promote, and search tools into the MCP server</name>
  <files>src/turbo_memory_mcp/server.py</files>
  <read_first>.planning/phases/02-namespace-model/02-CONTEXT.md, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/identity.py, src/turbo_memory_mcp/store.py</read_first>
  <action>Update `src/turbo_memory_mcp/server.py` to keep the existing `health`, `server_info`, `list_scopes`, and `self_test` tools, and add concrete namespace tools on the same stdio server: `remember_note(title, content, tags=None, source_refs=None, scope=\"project\")`, `promote_note(note_id)`, and `search_memory(query, scope=\"hybrid\", limit=5)`. `remember_note` must default to `scope=\"project\"` and reject ordinary direct global writes from callers. `promote_note` must read a project note, create a global copy or move with `promoted_from`, and return the promoted envelope. `search_memory` must support `project`, `global`, and `hybrid`, use the store from Plan 01, apply a deterministic project-bias ranking function in hybrid mode, and tie-break by project-preference, then `updated_at`, then stable item identity. Keep the ranking bonus itself in one isolated constant so future tuning does not require broad rewrites.</action>
  <verify>uv run pytest -q tests/test_namespace_tools.py -k \"remember or promote or search\"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/server.py` contains `remember_note`
    - `src/turbo_memory_mcp/server.py` contains `promote_note`
    - `src/turbo_memory_mcp/server.py` contains `search_memory`
    - `src/turbo_memory_mcp/server.py` contains `scope=\"project\"`
    - `src/turbo_memory_mcp/server.py` contains `hybrid`
  </acceptance_criteria>
  <done>The live MCP server supports safe namespace writes, explicit promotion, and deterministic project/global/hybrid queries.</done>
</task>

<task type="auto">
  <name>Task 3: Add namespace contract and precedence tests</name>
  <files>tests/test_tools.py, tests/test_namespace_tools.py</files>
  <read_first>src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/identity.py, src/turbo_memory_mcp/store.py, tests/test_tools.py</read_first>
  <action>Update `tests/test_tools.py` to assert the expanded tool catalog and namespace-aware `server_info`, `list_scopes`, and `self_test` payloads. Add `tests/test_namespace_tools.py` that seeds temporary project/global note data through the server or store layer and asserts: project writes succeed by default, direct global writes are rejected, promoted notes include `promoted_from`, `search_memory(scope=\"project\")` only returns project notes, `search_memory(scope=\"global\")` only returns global notes, and `search_memory(scope=\"hybrid\")` returns a deterministically ordered mixed result set with project hits first when relevance is close. Use temporary overrides such as `TQMEMORY_HOME`, `TQMEMORY_PROJECT_ID`, and `TQMEMORY_PROJECT_NAME` so the tests stay hermetic.</action>
  <verify>uv run pytest -q tests/test_tools.py tests/test_namespace_tools.py</verify>
  <acceptance_criteria>
    - `tests/test_namespace_tools.py` exists and references `remember_note`
    - `tests/test_namespace_tools.py` exists and references `promote_note`
    - `tests/test_namespace_tools.py` exists and references `search_memory`
    - `tests/test_namespace_tools.py` contains `promoted_from`
    - `uv run pytest -q tests/test_tools.py tests/test_namespace_tools.py` exits 0
  </acceptance_criteria>
  <done>The namespace contract is protected by automated tests that cover precedence, scope isolation, and provenance fields.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_tools.py tests/test_namespace_tools.py` succeeds
- [ ] The server tool catalog includes `remember_note`, `promote_note`, and `search_memory`
- [ ] `search_memory` supports `project`, `global`, and `hybrid`
</verification>

<success_criteria>

- All tasks completed
- The MCP server can store project notes, promote reusable notes, and query namespaces safely
- Hybrid precedence is deterministic and biased toward the current project
- Every returned item carries compact provenance and project-origin fields
</success_criteria>

<output>
After completion, create `.planning/phases/02-namespace-model/02-02-SUMMARY.md`
</output>
