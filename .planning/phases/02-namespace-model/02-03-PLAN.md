---
phase: 02-namespace-model
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - README.md
  - MEMORY_STRATEGY.md
  - examples/clients/SMOKE_CHECKLIST.md
  - scripts/smoke_test.py
  - tests/test_smoke_contract.py
autonomous: true
requirements: [SCP-01, SCP-02, SCP-03, SCP-04]
user_setup: []
must_haves:
  truths:
    - "Operators can see the real Phase 2 namespace behavior in docs without reverse engineering the code."
    - "The repo smoke path now validates namespace identity, note write/promotion, and hybrid query behavior over real MCP stdio."
    - "Published docs, smoke steps, and contract tests all agree on the same namespace vocabulary and metadata contract."
  artifacts:
    - "README.md"
    - "MEMORY_STRATEGY.md"
    - "examples/clients/SMOKE_CHECKLIST.md"
    - "scripts/smoke_test.py"
    - "tests/test_smoke_contract.py"
  key_links:
    - "`README.md` explains the implemented namespace model, not just the future intention."
    - "`scripts/smoke_test.py` exercises the same namespace behavior that the docs tell users to rely on."
    - "`tests/test_smoke_contract.py` locks the metadata contract exposed to real clients."
---

<objective>
Publish the Phase 2 namespace contract and align smoke validation with it.

Purpose: make the new namespace behavior visible, testable, and operationally trustworthy for real MCP clients.
Output: updated docs, updated smoke checklist, real stdio smoke coverage, and contract regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-namespace-model/02-CONTEXT.md
@.planning/phases/02-namespace-model/02-RESEARCH.md
@.planning/phases/02-namespace-model/02-VALIDATION.md
@README.md
@MEMORY_STRATEGY.md
@src/turbo_memory_mcp/contracts.py
@src/turbo_memory_mcp/server.py
@scripts/smoke_test.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator docs for the implemented namespace contract</name>
  <files>README.md, MEMORY_STRATEGY.md, examples/clients/SMOKE_CHECKLIST.md</files>
  <read_first>.planning/phases/02-namespace-model/02-CONTEXT.md, README.md, MEMORY_STRATEGY.md, examples/clients/SMOKE_CHECKLIST.md, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py</read_first>
  <action>Update `README.md` so it documents the implemented Phase 2 namespace contract, not only the Phase 1 reserved scopes. Add or revise sections to include: deterministic current-project resolution, central storage root `~/.turbo-quant-memory/`, default `project` writes, explicit promotion into `global`, supported query modes `project`/`global`/`hybrid`, and the compact default envelope fields. Update `MEMORY_STRATEGY.md` from a recommendation document into an implementation-aligned strategy reference that reflects the locked Phase 2 rules. Update `examples/clients/SMOKE_CHECKLIST.md` so the post-connect validation includes at least one real namespace flow: write a project note, promote a note, and run a `hybrid` query.</action>
  <verify>rg -n \"~/.turbo-quant-memory|remember_note|promote_note|search_memory|hybrid|project_id|promoted_from\" README.md MEMORY_STRATEGY.md examples/clients/SMOKE_CHECKLIST.md</verify>
  <acceptance_criteria>
    - `README.md` contains `~/.turbo-quant-memory`
    - `README.md` contains `remember_note`
    - `README.md` contains `promote_note`
    - `MEMORY_STRATEGY.md` contains `promoted_from`
    - `examples/clients/SMOKE_CHECKLIST.md` contains `search_memory`
  </acceptance_criteria>
  <done>The operator-facing docs describe the real Phase 2 namespace behavior with the same vocabulary and metadata contract used by the server.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the stdio smoke path and contract regression coverage</name>
  <files>scripts/smoke_test.py, tests/test_smoke_contract.py</files>
  <read_first>scripts/smoke_test.py, tests/test_smoke_contract.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, README.md</read_first>
  <action>Update `scripts/smoke_test.py` so it still launches `uv run turbo-memory-mcp serve` but now validates the Phase 2 namespace behavior end-to-end. The script must initialize the MCP session, assert the expanded tool catalog, confirm `server_info` exposes current-project and storage-root metadata, create a project note through `remember_note`, promote it with `promote_note`, and query it through `search_memory(scope=\"hybrid\")`. Update `tests/test_smoke_contract.py` so it locks the published namespace metadata: `server_info.current_project`, scope modes, compact default envelope fields, and the presence of `promoted_from` only when relevant. Keep the smoke output compact and pass/fail oriented.</action>
  <verify>uv run python scripts/smoke_test.py && uv run pytest -q tests/test_smoke_contract.py</verify>
  <acceptance_criteria>
    - `scripts/smoke_test.py` contains `remember_note`
    - `scripts/smoke_test.py` contains `promote_note`
    - `scripts/smoke_test.py` contains `search_memory`
    - `tests/test_smoke_contract.py` contains `current_project`
    - `tests/test_smoke_contract.py` contains `promoted_from`
    - `uv run python scripts/smoke_test.py && uv run pytest -q tests/test_smoke_contract.py` exits 0
  </acceptance_criteria>
  <done>The repo now smoke-tests the actual Phase 2 namespace contract over real MCP stdio and protects that contract with regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "~/.turbo-quant-memory|remember_note|promote_note|search_memory" README.md MEMORY_STRATEGY.md examples/clients/SMOKE_CHECKLIST.md` succeeds
- [ ] `uv run python scripts/smoke_test.py` succeeds
- [ ] `uv run pytest -q tests/test_smoke_contract.py` succeeds
</verification>

<success_criteria>

- All tasks completed
- Docs explain Phase 2 namespace behavior clearly enough for operators and downstream agents
- Smoke validation proves the namespace contract over real stdio MCP traffic
- Contract regression tests lock the metadata that clients and docs depend on
</success_criteria>

<output>
After completion, create `.planning/phases/02-namespace-model/02-03-SUMMARY.md`
</output>
