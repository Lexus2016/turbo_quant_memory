---
phase: 05-hydration-and-write-back
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - README.md
  - TECHNICAL_SPEC.md
  - MEMORY_STRATEGY.md
  - examples/clients/SMOKE_CHECKLIST.md
  - tests/test_tools.py
  - tests/test_smoke_contract.py
  - scripts/smoke_test.py
autonomous: true
requirements: [RET-03, RET-04, MEM-01, MEM-02]
user_setup: []
must_haves:
  truths:
    - "Published docs must describe Phase 5 hydration and typed write-back exactly as implemented."
    - "Contract tests and the real stdio smoke path must prove the new tool catalog and hydration payload shape."
    - "The smoke path must stay compact while proving index -> search -> hydrate -> write-back end to end."
  artifacts:
    - "README.md"
    - "tests/test_tools.py"
    - "tests/test_smoke_contract.py"
    - "scripts/smoke_test.py"
  key_links:
    - "`README.md`, `TECHNICAL_SPEC.md`, and `MEMORY_STRATEGY.md` explain the explicit hydration boundary and fixed note kinds."
    - "`tests/test_tools.py` and `tests/test_smoke_contract.py` freeze the published Phase 5 tool catalog and payload contracts."
    - "`scripts/smoke_test.py` proves the real stdio server can index, search, hydrate, and write typed notes."
---

<objective>
Publish and verify the Phase 5 hydration-and-write-back contract end to end.

Purpose: align the docs, contract tests, and real stdio smoke flow with the live Phase 5 server behavior.
Output: refreshed operator guidance, published contract tests, and an end-to-end hydration smoke path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hydration-and-write-back/05-CONTEXT.md
@.planning/phases/05-hydration-and-write-back/05-RESEARCH.md
@README.md
@TECHNICAL_SPEC.md
@MEMORY_STRATEGY.md
@tests/test_tools.py
@tests/test_smoke_contract.py
@scripts/smoke_test.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs and operator guidance for hydration plus typed notes</name>
  <files>README.md, TECHNICAL_SPEC.md, MEMORY_STRATEGY.md, examples/clients/SMOKE_CHECKLIST.md</files>
  <read_first>README.md, TECHNICAL_SPEC.md, MEMORY_STRATEGY.md, .planning/phases/05-hydration-and-write-back/05-CONTEXT.md, .planning/phases/05-hydration-and-write-back/05-RESEARCH.md</read_first>
  <action>Update the published docs so they describe the real Phase 5 contract: `semantic_search(...)` returns compact cards first, `hydrate(...)` is the explicit escalation path, Markdown hydration returns bounded file-local neighbors, and `remember_note(...)` now uses fixed note kinds. Refresh the smoke checklist so the manual flow proves `index_paths(...)`, `semantic_search(...)`, `hydrate(...)`, and typed note writes end to end.</action>
  <verify>rg -n "hydrate|note_kind|remember_note|semantic_search|related" README.md TECHNICAL_SPEC.md MEMORY_STRATEGY.md examples/clients/SMOKE_CHECKLIST.md</verify>
  <acceptance_criteria>
    - `README.md` contains `hydrate`
    - `README.md` contains `decision`
    - `TECHNICAL_SPEC.md` contains `hydrate`
    - `MEMORY_STRATEGY.md` contains `note_kind`
    - `examples/clients/SMOKE_CHECKLIST.md` contains `hydrate`
  </acceptance_criteria>
  <done>The published guidance now matches the live Phase 5 hydration-and-write-back contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the Phase 5 tool catalog and hydration payload contract</name>
  <files>tests/test_tools.py, tests/test_smoke_contract.py</files>
  <read_first>tests/test_tools.py, tests/test_smoke_contract.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py</read_first>
  <action>Update `tests/test_tools.py` so the expected tool catalog and `self_test` payload reflect the live Phase 5 surface, including `hydrate`. Update `tests/test_smoke_contract.py` so the published contract covers note-kind envelopes and hydration payload fields such as `mode`, `item`, `neighbors_before`, `neighbors_after`, and `neighbor_window`, while keeping the assertions focused on the public schema rather than internal ranking logic.</action>
  <verify>uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py</verify>
  <acceptance_criteria>
    - `tests/test_tools.py` contains `hydrate`
    - `tests/test_tools.py` contains `PHASE_5_TOOL_NAMES`
    - `tests/test_smoke_contract.py` contains `neighbors_before`
    - `tests/test_smoke_contract.py` contains `note_kind`
    - `uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py` exits 0
  </acceptance_criteria>
  <done>The published tests now freeze the real Phase 5 public API and hydration envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the real stdio smoke path for hydration and typed write-back</name>
  <files>scripts/smoke_test.py</files>
  <read_first>scripts/smoke_test.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/hydration.py, README.md</read_first>
  <action>Update `scripts/smoke_test.py` so the real stdio flow indexes temporary Markdown documents, writes a typed note, promotes it, runs `semantic_search(...)`, and then calls `hydrate(...)` on the winning Markdown hit. Assert that the hydration payload contains the full source content, deterministic neighbor windows, and no whole-file dump behavior. Keep the smoke output compact and pass/fail oriented.</action>
  <verify>uv run python scripts/smoke_test.py</verify>
  <acceptance_criteria>
    - `scripts/smoke_test.py` contains `hydrate`
    - `scripts/smoke_test.py` contains `note_kind`
    - `scripts/smoke_test.py` contains `neighbors_before`
    - `scripts/smoke_test.py` contains `neighbors_after`
    - `uv run python scripts/smoke_test.py` exits 0
  </acceptance_criteria>
  <done>The repo smoke path now proves Phase 5 end to end over the real stdio MCP server.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_tools.py tests/test_smoke_contract.py` succeeds
- [ ] `uv run python scripts/smoke_test.py` succeeds
- [ ] Docs and smoke guidance describe hydration and typed note writes accurately
</verification>

<success_criteria>

- All tasks completed
- Published docs describe the Phase 5 contract accurately
- Contract tests freeze the Phase 5 tool surface and hydration payload
- The stdio smoke path proves index -> search -> hydrate -> typed write-back end to end
</success_criteria>

<output>
After completion, create `.planning/phases/05-hydration-and-write-back/05-03-SUMMARY.md`
</output>
