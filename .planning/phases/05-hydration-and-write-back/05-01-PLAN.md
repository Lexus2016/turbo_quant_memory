---
phase: 05-hydration-and-write-back
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turbo_memory_mcp/store.py
  - src/turbo_memory_mcp/hydration.py
  - tests/test_storage.py
  - tests/test_hydration.py
autonomous: true
requirements: [RET-03, RET-04]
user_setup: []
must_haves:
  truths:
    - "Hydration resolves an existing search item by `item_id` and `scope` instead of inventing a second retrieval identity model."
    - "Default hydration is bounded and deterministic: the winning Markdown block plus a small symmetric local neighborhood."
    - "Hydrating a memory note returns the full curated note without artificial neighbor expansion."
  artifacts:
    - "src/turbo_memory_mcp/store.py"
    - "src/turbo_memory_mcp/hydration.py"
    - "tests/test_hydration.py"
  key_links:
    - "`src/turbo_memory_mcp/store.py` owns the canonical note and Markdown files plus the neighborhood helpers hydration needs."
    - "`src/turbo_memory_mcp/hydration.py` owns the explicit `hydrate(...)` escalation flow and mode handling."
    - "`tests/test_storage.py` and `tests/test_hydration.py` protect neighborhood lookup and bounded payload behavior before the MCP surface depends on it."
---

<objective>
Implement the hydration foundation for Phase 5.

Purpose: recover fuller source-local context only when `semantic_search(...)` is insufficient, while preserving the compressed-first default established in Phase 4.
Output: canonical hydration helpers for Markdown and notes, bounded neighborhood lookup, and focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hydration-and-write-back/05-CONTEXT.md
@.planning/phases/05-hydration-and-write-back/05-RESEARCH.md
@.planning/phases/05-hydration-and-write-back/05-VALIDATION.md
@src/turbo_memory_mcp/store.py
@src/turbo_memory_mcp/retrieval.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic item lookup and Markdown neighborhood helpers</name>
  <files>src/turbo_memory_mcp/store.py, tests/test_storage.py</files>
  <read_first>.planning/phases/05-hydration-and-write-back/05-RESEARCH.md, src/turbo_memory_mcp/store.py, tests/test_storage.py</read_first>
  <action>Extend `src/turbo_memory_mcp/store.py` with the minimum helper surface needed for hydration: deterministic project item lookup by `item_id`, Markdown block neighborhood lookup ordered by `chunk_index`, and note-kind persistence defaults that can survive promotion and later retrieval. Keep the JSON files as the only source of truth and do not move hydration state into LanceDB. Update `tests/test_storage.py` to prove note metadata persists and neighborhood lookup returns the expected before/after slices for a file.</action>
  <verify>uv run pytest -q tests/test_storage.py -k "note_kind or neighborhood"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/store.py` contains `note_kind`
    - `src/turbo_memory_mcp/store.py` contains `read_markdown_neighborhood`
    - `tests/test_storage.py` contains `note_kind`
    - `tests/test_storage.py` contains `neighborhood`
  </acceptance_criteria>
  <done>The store can now resolve hydrated items and bounded local Markdown context without changing the persistence model.</done>
</task>

<task type="auto">
  <name>Task 2: Implement hydration orchestration for Markdown blocks and notes</name>
  <files>src/turbo_memory_mcp/hydration.py, tests/test_hydration.py</files>
  <read_first>.planning/phases/05-hydration-and-write-back/05-CONTEXT.md, src/turbo_memory_mcp/store.py, src/turbo_memory_mcp/retrieval.py</read_first>
  <action>Create `src/turbo_memory_mcp/hydration.py` with one narrow `hydrate(...)` orchestration path that accepts `item_id`, `scope`, and `mode`. Support at least `default` and `related` modes. For Markdown hits, return the full target block plus deterministic symmetric neighbors from the same file. For note hits, return the full note body plus note metadata and no artificial neighbors. Add `tests/test_hydration.py` to lock the payload shape, mode windows, and note-versus-Markdown behavior.</action>
  <verify>uv run pytest -q tests/test_hydration.py</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/hydration.py` contains `def hydrate(`
    - `src/turbo_memory_mcp/hydration.py` contains `default`
    - `src/turbo_memory_mcp/hydration.py` contains `related`
    - `tests/test_hydration.py` contains `neighbors_before`
    - `tests/test_hydration.py` contains `neighbors_after`
  </acceptance_criteria>
  <done>The repo now has a deterministic explicit hydration layer for both source blocks and curated notes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_storage.py tests/test_hydration.py` succeeds
- [ ] Markdown hydration stays file-local and deterministic
- [ ] Note hydration returns full note content without artificial neighbor expansion
</verification>

<success_criteria>

- All tasks completed
- `hydrate(...)` can recover fuller Markdown context on demand
- `hydrate(...)` can recover related local neighbors without dumping the whole file
- Hydration stays explicit, bounded, and source-traceable
</success_criteria>

<output>
After completion, create `.planning/phases/05-hydration-and-write-back/05-01-SUMMARY.md`
</output>
