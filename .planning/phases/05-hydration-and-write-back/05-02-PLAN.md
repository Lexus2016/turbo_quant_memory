---
phase: 05-hydration-and-write-back
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/turbo_memory_mcp/contracts.py
  - src/turbo_memory_mcp/retrieval_index.py
  - src/turbo_memory_mcp/retrieval.py
  - src/turbo_memory_mcp/server.py
  - tests/test_namespace_tools.py
  - tests/test_semantic_search.py
  - tests/test_retrieval_index.py
autonomous: true
requirements: [RET-03, RET-04, MEM-01, MEM-02]
user_setup: []
must_haves:
  truths:
    - "Phase 5 adds exactly one new public MCP tool: `hydrate(...)`."
    - "Write-back stays explicit and curated through fixed note kinds rather than one undifferentiated note type."
    - "Typed notes remain searchable beside Markdown content without weakening Phase 4 ranking rules."
  artifacts:
    - "src/turbo_memory_mcp/server.py"
    - "src/turbo_memory_mcp/contracts.py"
    - "src/turbo_memory_mcp/retrieval.py"
    - "tests/test_namespace_tools.py"
  key_links:
    - "`src/turbo_memory_mcp/server.py` owns the live MCP tool registration and note-write validation."
    - "`src/turbo_memory_mcp/retrieval.py` keeps the compact search contract and now exposes note-kind metadata without bloating Markdown cards."
    - "`tests/test_namespace_tools.py`, `tests/test_semantic_search.py`, and `tests/test_retrieval_index.py` protect typed notes, searchability, and the live `hydrate(...)` surface."
---

<objective>
Wire the hydration and typed write-back contract into the live Phase 5 MCP server.

Purpose: expose `hydrate(...)` as the canonical escalation path and make note writes first-class through a small fixed note-kind enum.
Output: updated tool catalog, typed note persistence and retrieval metadata, and contract tests for search plus hydration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hydration-and-write-back/05-CONTEXT.md
@.planning/phases/05-hydration-and-write-back/05-RESEARCH.md
@src/turbo_memory_mcp/contracts.py
@src/turbo_memory_mcp/retrieval_index.py
@src/turbo_memory_mcp/retrieval.py
@src/turbo_memory_mcp/server.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend note records and retrieval rows with fixed note kinds</name>
  <files>src/turbo_memory_mcp/retrieval_index.py, src/turbo_memory_mcp/retrieval.py, tests/test_retrieval_index.py, tests/test_semantic_search.py</files>
  <read_first>src/turbo_memory_mcp/retrieval_index.py, src/turbo_memory_mcp/retrieval.py, .planning/phases/05-hydration-and-write-back/05-RESEARCH.md</read_first>
  <action>Persist a fixed note-kind enum across the retrieval layer using the locked values `decision`, `lesson`, `handoff`, and `pattern`. Extend retrieval rows, search payload shaping, and tests so notes expose `note_kind` while Markdown cards stay compact. Preserve the existing project bias and markdown-first ordering rules from Phase 4.</action>
  <verify>uv run pytest -q tests/test_retrieval_index.py tests/test_semantic_search.py -k "note_kind"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/retrieval_index.py` contains `note_kind`
    - `src/turbo_memory_mcp/retrieval.py` contains `note_kind`
    - `tests/test_semantic_search.py` contains `note_kind`
  </acceptance_criteria>
  <done>Typed notes are now indexed and surfaced as a first-class retrieval concept without changing the compressed Markdown contract.</done>
</task>

<task type="auto">
  <name>Task 2: Export `hydrate(...)` and typed `remember_note(...)` through the MCP server</name>
  <files>src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, tests/test_namespace_tools.py</files>
  <read_first>src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/hydration.py, .planning/phases/05-hydration-and-write-back/05-CONTEXT.md</read_first>
  <action>Update the public MCP contract so `hydrate(item_id, scope, mode=\"default\")` is available in the live tool catalog and `remember_note(...)` requires a valid fixed note kind. Refresh `self_test` and contract helpers to publish the Phase 5 tool list and typed note envelopes. Update `tests/test_namespace_tools.py` to cover invalid note kinds, hydrated Markdown results, and hydrated note results through the live impl helpers.</action>
  <verify>uv run pytest -q tests/test_namespace_tools.py -k "hydrate or note_kind"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/contracts.py` contains `PHASE_5_TOOL_NAMES`
    - `src/turbo_memory_mcp/server.py` contains `def hydrate(`
    - `src/turbo_memory_mcp/server.py` contains `note_kind`
    - `tests/test_namespace_tools.py` contains `hydrate_impl`
  </acceptance_criteria>
  <done>The live MCP surface now supports explicit hydration and enforces typed note writes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_namespace_tools.py tests/test_semantic_search.py tests/test_retrieval_index.py` succeeds
- [ ] The public tool catalog includes `hydrate`
- [ ] `remember_note(...)` rejects unknown note kinds
</verification>

<success_criteria>

- All tasks completed
- Agents can write back typed notes and later retrieve them with note-kind metadata
- Agents can explicitly hydrate fuller context through one canonical MCP tool
- Search and hydration contracts stay consistent across runtime and tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-hydration-and-write-back/05-02-SUMMARY.md`
</output>
