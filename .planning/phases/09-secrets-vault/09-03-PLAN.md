---
phase: 09-secrets-vault
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - scripts/smoke_test.py
  - README.md
  - README.uk.md
  - README.ru.md
  - TECHNICAL_SPEC.md
  - TECHNICAL_SPEC.uk.md
  - TECHNICAL_SPEC.ru.md
  - MEMORY_STRATEGY.md
  - MEMORY_STRATEGY.uk.md
  - MEMORY_STRATEGY.ru.md
  - CHANGELOG.md
  - AGENTS.md
  - CLAUDE.md
  - examples/clients/
autonomous: true
requirements: [VLT-06]
user_setup:
  - "Set master key via either: `keyring set turbo-quant-memory secrets-master-<project_id>` OR `export TQMEMORY_SECRETS_PASSPHRASE=<long-passphrase>` in shell rc."
---

<objective>
Make the secrets vault officially adopt-ready: extend the smoke test to cover the new tool surface, document the feature across all bilingual docs (README, TECHNICAL_SPEC, MEMORY_STRATEGY, AGENTS, CLAUDE), bump the CHANGELOG, and update client example fixtures so live agents discover the tools.
</objective>

<tasks>

1. Extend `scripts/smoke_test.py`:
   - After existing index/search/hydrate flow, add a `set_secret` -> `get_secret` -> `list_secrets` -> `delete_secret` round-trip against a temp project.
   - Use a smoke-only env var `TQMEMORY_SECRETS_PASSPHRASE=smoke-test-passphrase-only` to avoid any real keyring writes.
   - Assert `secret_value` is present in `get_secret` response, NOT in `list_secrets`, and NOT in any descriptive field.
   - Print a one-line success summary identical in shape to existing smoke output.

2. Documentation updates (bilingual EN/UK + RU per project convention) — written in a **reassurance tone for existing users** so nobody worries that the upgrade breaks their existing memory:
   - `README.*`: new top-level section "Secrets Vault / Сховище секретів" with this exact structure:
     1. **Why we built this** — the cross-session credential re-paste pain.
     2. **What changes** — four new opt-in MCP tools (`set_secret`/`get_secret`/`list_secrets`/`delete_secret`), tool count 14 → 18.
     3. **What does NOT change** — existing notes, `semantic_search`, `hydrate`, `lint_knowledge_base` are byte-identical in behavior; no migration of existing data; the vault auto-provisions empty so zero impact if you never call `set_secret`.
     4. **How to use** — one-time master-key setup (env var or `keyring set`), then four-tool round-trip example.
     5. **Threat model** — what we protect (accidental backup, share-screen, `git add`) and what we explicitly do not (root compromise, live daemon takeover).
     6. **FAQ** — "Is my existing memory affected?" (no), "Do I need to do anything?" (no, unless you want to start using secrets), "Where are secrets stored?" (per-project encrypted blob under `~/.turbo-quant-memory/projects/<id>/secrets/`), "Can my secrets leak into `semantic_search`?" (no, hard isolation), "What if I just want this feature off?" (it already is — the vault is empty until you populate it).
   - `TECHNICAL_SPEC.*`: extend "MCP Tool Surface" table with the four new tools; extend "Security and Trust" with the threat model (in-scope: accidental backup/screen/git add; out-of-scope: root compromise / live daemon takeover); add a "Secrets" subsection under "Data Model" describing `vault.tqv` and `meta.json` layout.
   - `MEMORY_STRATEGY.*`: add a "Secrets vs. Notes" subsection — secrets MUST be accessed only by exact-name `get_secret`, never via `semantic_search`/`hydrate`; rationale.
   - `AGENTS.md` and project `CLAUDE.md`: add an "Accessing project secrets" recipe — `semantic_search` for a `pattern`-kind recipe note, then `get_secret(name)` to fetch the value. Explicit rule: NEVER `set_secret` with a value that came from chat context where it can be logged; ask the user to set it directly the first time, then reference by name.

3. `CHANGELOG.md`: new entry `[0.7.0]` (or next available version per current version tracking) titled "Project-scoped encrypted secrets vault". Bullets:
   - Added: 4 MCP tools, `Subsystem.SECRETS` migration v0->v1, `keyring`/`cryptography`/`argon2-cffi` deps, per-project AES-256-GCM vault.
   - Changed: `self_test.tool_count` 14 -> 18; `server_info()` payload gains `secrets` block.
   - Security: documented threat model, indexer/lint/retrieval refuse `secrets/` traversal.
   - Migration: `turbo-memory-mcp migrate --apply` after closing all MCP clients (existing lockfile mechanic). Provisions empty vault per existing project.

4. Client fixtures under `examples/clients/`: append `set_secret`/`get_secret` example calls to each client snippet (Claude Code, Codex, Cursor, OpenCode) so live users see them in autocompletion.

5. Run full smoke + lint + tests once more end-to-end; record final tool count, file count, and migration run-time in `09-03-SUMMARY.md`.

</tasks>

<verification>
- [ ] `uv run ruff check src tests scripts`
- [ ] `uv run pytest -q`
- [ ] `uv run python scripts/smoke_test.py` (full path with new secrets section green)
- [ ] All `*.md` files updated in EN/UK/RU; no untranslated stubs.
- [ ] `CHANGELOG.md` lints (if a linter is configured) and tool-count claim matches reality.
</verification>

<output>
After completion, create `.planning/phases/09-secrets-vault/09-03-SUMMARY.md` and update `.planning/STATE.md` to mark Phase 9 complete and reference any follow-up todos (e.g. master-key rotation).
</output>
