---
phase: 03-markdown-ingestion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/turbo_memory_mcp/markdown_parser.py
  - tests/test_markdown_chunking.py
autonomous: true
requirements: [ING-02]
user_setup: []
must_haves:
  truths:
    - "Markdown files are chunked by headings first, with deterministic fallback splitting for oversized sections."
    - "Block identity is location-based and stays stable when content changes but section location does not."
    - "The parser handles pre-heading content through a synthetic preamble section instead of dropping it."
  artifacts:
    - "pyproject.toml"
    - "src/turbo_memory_mcp/markdown_parser.py"
    - "tests/test_markdown_chunking.py"
  key_links:
    - "`src/turbo_memory_mcp/markdown_parser.py` uses `markdown-it-py` and defines the heading-aware chunking contract for later ingestion."
    - "`tests/test_markdown_chunking.py` protects preamble handling, fallback splitting, and stable block identity."
    - "This plan keeps Phase 3 narrow by adding parsing only, not vector retrieval."
---

<objective>
Implement the deterministic Markdown parsing and chunking contract for Phase 3.

Purpose: turn raw Markdown files into stable block candidates that later ingestion code can persist and reindex incrementally.
Output: `markdown-it-py` dependency, a pure parser/chunker module, and focused unit tests for heading-aware chunking and stable IDs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-markdown-ingestion/03-RESEARCH.md
@.planning/phases/03-markdown-ingestion/03-VALIDATION.md
@TECHNICAL_SPEC.md
@pyproject.toml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Markdown parser dependency and stable parser module scaffold</name>
  <files>pyproject.toml, src/turbo_memory_mcp/markdown_parser.py</files>
  <read_first>.planning/phases/03-markdown-ingestion/03-RESEARCH.md, pyproject.toml, AGENTS.md</read_first>
  <action>Update `pyproject.toml` to add `markdown-it-py>=4.0.0,<5.0` as the only new Phase 3 dependency. Create `src/turbo_memory_mcp/markdown_parser.py` with a pure parsing API that does not touch MCP or filesystem code. Implement exact public shapes for a parsed block candidate, including `heading_path`, `chunk_index`, `content_raw`, `block_checksum`, and enough location inputs to derive a stable `block_id` later. Use `MarkdownIt(\"commonmark\")` plus `SyntaxTreeNode` from `markdown-it-py` as the parser entrypoint. Include a synthetic `__preamble__` section for content before the first heading.</action>
  <verify>uv run pytest -q tests/test_markdown_chunking.py -k "preamble or parser"</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `markdown-it-py`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `MarkdownIt("commonmark")`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `SyntaxTreeNode`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `__preamble__`
  </acceptance_criteria>
  <done>The repo has the official Markdown parser dependency and a pure module for deterministic chunk preparation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement heading-aware chunking with size-based fallback</name>
  <files>src/turbo_memory_mcp/markdown_parser.py</files>
  <read_first>.planning/phases/03-markdown-ingestion/03-RESEARCH.md, src/turbo_memory_mcp/markdown_parser.py, TECHNICAL_SPEC.md</read_first>
  <action>Implement a concrete chunking function in `src/turbo_memory_mcp/markdown_parser.py` that: parses a Markdown document once; builds ordered sections from heading tokens; preserves heading paths as lists; keeps normal sections whole when they are below a soft limit of `1000` characters; applies fallback splitting only inside oversized sections; and tolerates a single oversized block instead of inventing fragile code-fence splitting. Add a deterministic location-based `build_block_id(root_id, relative_source_path, heading_path, chunk_index)` helper so block identity comes from logical position, not content hash.</action>
  <verify>uv run pytest -q tests/test_markdown_chunking.py -k "headings or fallback or block_id"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/markdown_parser.py` contains `1000`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `build_block_id`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `heading_path`
    - `src/turbo_memory_mcp/markdown_parser.py` contains `chunk_index`
  </acceptance_criteria>
  <done>The parser can now produce stable heading-aware block candidates with deterministic fallback splitting.</done>
</task>

<task type="auto">
  <name>Task 3: Lock chunking behaviour with focused unit tests</name>
  <files>tests/test_markdown_chunking.py</files>
  <read_first>src/turbo_memory_mcp/markdown_parser.py, .planning/phases/03-markdown-ingestion/03-VALIDATION.md, AGENTS.md</read_first>
  <action>Create `tests/test_markdown_chunking.py` with exact cases for: preamble content before the first heading; nested heading paths like `Architecture > Storage`; fallback splitting of one oversized section into multiple chunks; and stable `block_id` values when section content changes but `root_id`, `relative_source_path`, `heading_path`, and `chunk_index` stay the same. Use small inline Markdown fixtures that include headings, lists, and fenced code blocks so the tests prove the parser is not relying on regex-only splitting.</action>
  <verify>uv run pytest -q tests/test_markdown_chunking.py</verify>
  <acceptance_criteria>
    - `tests/test_markdown_chunking.py` contains `Architecture`
    - `tests/test_markdown_chunking.py` contains `__preamble__`
    - `tests/test_markdown_chunking.py` contains `block_id`
    - `tests/test_markdown_chunking.py` contains ``````
    - `uv run pytest -q tests/test_markdown_chunking.py` exits 0
  </acceptance_criteria>
  <done>The chunking contract is protected by deterministic tests before live indexing depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_markdown_chunking.py` succeeds
- [ ] `markdown-it-py` is the only new Phase 3 parsing dependency
- [ ] Block IDs are location-based rather than content-hash-based
</verification>

<success_criteria>

- All tasks completed
- The repo can parse Markdown into stable heading-aware block candidates
- Oversized sections split deterministically without regex-only heuristics
- Stable block IDs survive normal content edits at the same logical location
</success_criteria>

<output>
After completion, create `.planning/phases/03-markdown-ingestion/03-02-SUMMARY.md`
</output>
