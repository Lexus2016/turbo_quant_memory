---
phase: 03-markdown-ingestion
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/turbo_memory_mcp/ingestion.py
  - src/turbo_memory_mcp/contracts.py
  - src/turbo_memory_mcp/server.py
  - tests/test_tools.py
  - tests/test_ingestion_tools.py
  - tests/test_smoke_contract.py
  - scripts/smoke_test.py
autonomous: true
requirements: [ING-01, ING-02, ING-03]
user_setup: []
must_haves:
  truths:
    - "Agents can register one or more Markdown directories and index them through the same stdio MCP server."
    - "Incremental indexing skips untouched files, rewrites only changed files, and cleans up deleted-file artifacts."
    - "The live MCP contract and smoke path prove Markdown ingestion without pulling in vector retrieval."
  artifacts:
    - "src/turbo_memory_mcp/ingestion.py"
    - "src/turbo_memory_mcp/contracts.py"
    - "src/turbo_memory_mcp/server.py"
    - "tests/test_ingestion_tools.py"
    - "scripts/smoke_test.py"
  key_links:
    - "`src/turbo_memory_mcp/ingestion.py` orchestrates root registration, file scanning, chunking, and incremental refresh."
    - "`src/turbo_memory_mcp/server.py` exposes `index_paths(paths=None, mode=\"incremental\")` on the current MCP server."
    - "`scripts/smoke_test.py` validates a real temporary Markdown index plus one incremental rerun."
---

<objective>
Wire the Markdown ingestion pipeline into the live MCP contract.

Purpose: expose a real `index_paths` flow that registers Markdown roots, persists blocks, and supports changed-files-only refresh over the current stdio server.
Output: ingestion orchestrator, live MCP tool integration, contract tests, and smoke validation for one incremental cycle.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-markdown-ingestion/03-RESEARCH.md
@.planning/phases/03-markdown-ingestion/03-VALIDATION.md
@src/turbo_memory_mcp/store.py
@src/turbo_memory_mcp/markdown_parser.py
@src/turbo_memory_mcp/contracts.py
@src/turbo_memory_mcp/server.py
@scripts/smoke_test.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement project-scoped Markdown ingestion orchestration</name>
  <files>src/turbo_memory_mcp/ingestion.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, tests/test_tools.py</files>
  <read_first>.planning/phases/03-markdown-ingestion/03-RESEARCH.md, .planning/phases/03-markdown-ingestion/03-VALIDATION.md, src/turbo_memory_mcp/store.py, src/turbo_memory_mcp/markdown_parser.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py</read_first>
  <action>Create `src/turbo_memory_mcp/ingestion.py` with a project-scoped orchestration API that: normalizes and registers root paths; derives deterministic `root_id` and `file_key` values; walks `*.md` files; uses the parser from `src/turbo_memory_mcp/markdown_parser.py`; compares `size + mtime_ns` before reading unchanged files; confirms changed files with `source_checksum`; rewrites only changed file manifests and blocks; and deletes manifests/blocks for removed files. Update `src/turbo_memory_mcp/contracts.py` to add `index_paths` to the tool catalog and define an indexing result payload with exact fields `mode`, `registered_roots`, `indexed_files`, `changed_files`, `skipped_files`, `deleted_files`, and `block_count`. Update `src/turbo_memory_mcp/server.py` to expose `index_paths(paths: list[str] | None = None, mode: str = "incremental")` on the existing stdio server, defaulting to previously registered roots when `paths` is omitted. Update `tests/test_tools.py` so the live tool catalog and `self_test` payload include `index_paths`.</action>
  <verify>uv run pytest -q tests/test_tools.py -k "tool_catalog or self_test"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/ingestion.py` contains `index_paths`
    - `src/turbo_memory_mcp/ingestion.py` contains `mtime_ns`
    - `src/turbo_memory_mcp/ingestion.py` contains `source_checksum`
    - `src/turbo_memory_mcp/contracts.py` contains `registered_roots`
    - `src/turbo_memory_mcp/server.py` contains `index_paths(paths`
    - `tests/test_tools.py` contains `index_paths`
  </acceptance_criteria>
  <done>The live MCP contract can now trigger Markdown indexing through a project-scoped orchestration layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add ingestion contract tests for registration and incremental refresh</name>
  <files>tests/test_ingestion_tools.py, tests/test_smoke_contract.py</files>
  <read_first>src/turbo_memory_mcp/ingestion.py, src/turbo_memory_mcp/contracts.py, src/turbo_memory_mcp/server.py, .planning/phases/03-markdown-ingestion/03-VALIDATION.md</read_first>
  <action>Create `tests/test_ingestion_tools.py` with hermetic temp-directory coverage for: registering one or more Markdown roots through the server or ingestion layer; writing block records for indexed Markdown files; rerunning `mode=\"incremental\"` with no content changes and asserting `skipped_files` increases while `changed_files` stays `0`; editing one file and asserting only that file is rewritten; and deleting one file and asserting `deleted_files` increments while unrelated file blocks survive. Update `tests/test_smoke_contract.py` so the published contract now expects `index_paths` in the tool catalog and locks the new indexing result fields `registered_roots`, `indexed_files`, `changed_files`, `skipped_files`, `deleted_files`, and `block_count`.</action>
  <verify>uv run pytest -q tests/test_ingestion_tools.py tests/test_smoke_contract.py</verify>
  <acceptance_criteria>
    - `tests/test_ingestion_tools.py` contains `mode=\"incremental\"`
    - `tests/test_ingestion_tools.py` contains `changed_files`
    - `tests/test_ingestion_tools.py` contains `deleted_files`
    - `tests/test_smoke_contract.py` contains `index_paths`
    - `tests/test_smoke_contract.py` contains `registered_roots`
    - `uv run pytest -q tests/test_ingestion_tools.py tests/test_smoke_contract.py` exits 0
  </acceptance_criteria>
  <done>The indexing contract is protected by automated tests for registration, incremental skip/rewrite, and deleted-file cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the stdio smoke path to validate one incremental Markdown cycle</name>
  <files>scripts/smoke_test.py</files>
  <read_first>scripts/smoke_test.py, src/turbo_memory_mcp/server.py, src/turbo_memory_mcp/ingestion.py, tests/test_ingestion_tools.py, README.md</read_first>
  <action>Update `scripts/smoke_test.py` so the existing temp-home smoke run also creates a temporary Markdown directory with at least two `.md` files, calls `index_paths(paths=[...], mode=\"full\")`, reruns `index_paths(mode=\"incremental\")` with no content changes, edits one Markdown file, deletes the other file, and confirms the second incremental run reports non-zero `changed_files` and `deleted_files` while keeping the output compact and pass/fail-oriented. Preserve the Phase 2 namespace checks, but make the smoke summary explicitly mention Markdown indexing counts.</action>
  <verify>uv run python scripts/smoke_test.py</verify>
  <acceptance_criteria>
    - `scripts/smoke_test.py` contains `index_paths`
    - `scripts/smoke_test.py` contains `mode=\"full\"`
    - `scripts/smoke_test.py` contains `mode=\"incremental\"`
    - `scripts/smoke_test.py` contains `changed_files`
    - `scripts/smoke_test.py` contains `deleted_files`
    - `uv run python scripts/smoke_test.py` exits 0
  </acceptance_criteria>
  <done>The repo smoke path now proves a real Markdown indexing cycle over stdio, not just tool discovery.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_tools.py tests/test_ingestion_tools.py tests/test_smoke_contract.py` succeeds
- [ ] `uv run python scripts/smoke_test.py` succeeds
- [ ] `index_paths` supports registration plus incremental rerun without forcing a whole-corpus rebuild
</verification>

<success_criteria>

- All tasks completed
- The MCP server can register and index project Markdown roots
- Incremental reindex skips untouched files and cleans up deleted files
- Live tests and smoke validation prove the ingestion contract end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-markdown-ingestion/03-03-SUMMARY.md`
</output>
