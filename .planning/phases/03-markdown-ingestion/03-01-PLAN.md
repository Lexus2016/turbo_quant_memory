---
phase: 03-markdown-ingestion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turbo_memory_mcp/store.py
  - tests/test_ingestion_store.py
autonomous: true
requirements: [ING-01, ING-03]
user_setup: []
must_haves:
  truths:
    - "Project-scoped Markdown storage exists under `projects/<project_id>/markdown/` without weakening the Phase 2 note/global layout."
    - "Registered Markdown roots, per-file manifests, and stored blocks persist as atomic JSON records."
    - "The store exposes source-scoped cleanup helpers so incremental reindex can rewrite only changed or deleted files."
  artifacts:
    - "src/turbo_memory_mcp/store.py"
    - "tests/test_ingestion_store.py"
  key_links:
    - "`src/turbo_memory_mcp/store.py` owns the `markdown/{roots,files,blocks}` filesystem layout and persistence helpers."
    - "`tests/test_ingestion_store.py` proves root registration, manifest persistence, and targeted cleanup for changed files."
    - "This plan lays the storage foundation that `03-03` will use for the live `index_paths` tool."
---

<objective>
Create the Markdown ingestion storage foundation for Phase 3.

Purpose: extend the current filesystem-backed project store so registered Markdown roots, per-file manifests, and stored blocks can persist safely before the MCP tool layer is wired in.
Output: markdown storage helpers in `store.py` plus focused storage tests for roots, manifests, block records, and source-scoped cleanup.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-markdown-ingestion/03-RESEARCH.md
@.planning/phases/03-markdown-ingestion/03-VALIDATION.md
@TECHNICAL_SPEC.md
@MEMORY_STRATEGY.md
@src/turbo_memory_mcp/store.py
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the central store with Markdown root, file, and block layouts</name>
  <files>src/turbo_memory_mcp/store.py</files>
  <read_first>.planning/phases/03-markdown-ingestion/03-RESEARCH.md, .planning/phases/03-markdown-ingestion/03-VALIDATION.md, src/turbo_memory_mcp/store.py, .planning/phases/02-namespace-model/02-01-SUMMARY.md, AGENTS.md</read_first>
  <action>Update `src/turbo_memory_mcp/store.py` so the existing project-scoped store gains a Markdown subtree under `projects/<project_id>/markdown/`. Add exact path helpers for `markdown/manifest.json`, `markdown/roots/<root_id>.json`, `markdown/files/<file_key>.json`, and `markdown/blocks/<block_id>.json`. Define concrete JSON record shapes for: root records (`root_id`, `scope`, `project_id`, `path`, `path_hash`, `registered_at`, `updated_at`), file manifests (`root_id`, `source_path`, `file_key`, `size`, `mtime_ns`, `source_checksum`, `block_ids`, `indexed_at`), and markdown blocks (`block_id`, `scope`, `project_id`, `source_kind = "markdown"`, `root_id`, `source_path`, `heading_path`, `chunk_index`, `content_raw`, `block_checksum`, `source_checksum`, `updated_at`). Reuse the existing atomic JSON write helper. Add store methods to write/read/list root records, write/read/list file manifests, write/read/list block records, and delete block records for one `root_id + source_path` without touching other files or notes.</action>
  <verify>uv run pytest -q tests/test_ingestion_store.py -k "layout or roots or manifests"</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/store.py` contains `markdown/roots`
    - `src/turbo_memory_mcp/store.py` contains `markdown/files`
    - `src/turbo_memory_mcp/store.py` contains `markdown/blocks`
    - `src/turbo_memory_mcp/store.py` contains `source_checksum`
    - `src/turbo_memory_mcp/store.py` contains `delete_blocks_for_file`
  </acceptance_criteria>
  <done>The project store can now persist Markdown roots, file manifests, and block records in a deterministic project-local layout.</done>
</task>

<task type="auto">
  <name>Task 2: Add storage tests for root registration and manifest persistence</name>
  <files>tests/test_ingestion_store.py</files>
  <read_first>.planning/phases/03-markdown-ingestion/03-VALIDATION.md, src/turbo_memory_mcp/store.py, tests/test_storage.py, AGENTS.md</read_first>
  <action>Create `tests/test_ingestion_store.py` with hermetic tests using `TQMEMORY_HOME`, `TQMEMORY_PROJECT_ID`, and `TQMEMORY_PROJECT_NAME`. Add exact coverage for: root-record persistence under `projects/<project_id>/markdown/roots/`, file-manifest persistence under `markdown/files/`, block-record persistence under `markdown/blocks/`, and JSON payload fields `root_id`, `file_key`, `source_path`, `block_id`, `source_checksum`, and `block_ids`. Use deterministic fixture values such as `root_id = "docs-root"` and `file_key = "docs-architecture-adr-001"` so the tests are grep-verifiable.</action>
  <verify>uv run pytest -q tests/test_ingestion_store.py -k "roots or manifests or blocks"</verify>
  <acceptance_criteria>
    - `tests/test_ingestion_store.py` contains `docs-root`
    - `tests/test_ingestion_store.py` contains `source_checksum`
    - `tests/test_ingestion_store.py` contains `block_ids`
    - `tests/test_ingestion_store.py` contains `markdown/blocks`
    - `uv run pytest -q tests/test_ingestion_store.py -k "roots or manifests or blocks"` exits 0
  </acceptance_criteria>
  <done>The repo has automated coverage for the Phase 3 Markdown storage primitives before incremental orchestration depends on them.</done>
</task>

<task type="auto">
  <name>Task 3: Add targeted cleanup coverage for changed and deleted files</name>
  <files>src/turbo_memory_mcp/store.py, tests/test_ingestion_store.py</files>
  <read_first>src/turbo_memory_mcp/store.py, tests/test_ingestion_store.py, .planning/phases/03-markdown-ingestion/03-RESEARCH.md</read_first>
  <action>Expand the store and tests so one source file can be rewritten or deleted without forcing a whole-corpus rebuild. Add an explicit helper in `src/turbo_memory_mcp/store.py` that replaces the block set for one `root_id + source_path`, removes stale block files for that source only, and deletes the file manifest when the source disappears. Add tests that seed two Markdown files under the same root, rewrite one file, delete the other file, and assert that unaffected block records survive while only the targeted file's manifest and blocks change.</action>
  <verify>uv run pytest -q tests/test_ingestion_store.py</verify>
  <acceptance_criteria>
    - `src/turbo_memory_mcp/store.py` contains `replace_blocks_for_file`
    - `tests/test_ingestion_store.py` contains `delete_file_manifest`
    - `tests/test_ingestion_store.py` contains `unaffected`
    - `uv run pytest -q tests/test_ingestion_store.py` exits 0
  </acceptance_criteria>
  <done>The storage layer now supports source-scoped rewrites and deletes required for changed-files-only indexing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest -q tests/test_ingestion_store.py` succeeds
- [ ] Markdown storage lives under `projects/<project_id>/markdown/{roots,files,blocks}`
- [ ] One file can be cleaned up without deleting unrelated blocks
</verification>

<success_criteria>

- All tasks completed
- Project-scoped Markdown storage exists without touching `global`
- Root, file, and block records persist atomically
- Source-scoped cleanup exists for changed/deleted file refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/03-markdown-ingestion/03-01-SUMMARY.md`
</output>
